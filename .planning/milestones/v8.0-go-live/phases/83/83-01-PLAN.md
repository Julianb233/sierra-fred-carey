---
phase: 83-founder-mindset-monitor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sentiment/stress-detector.ts
  - lib/sentiment/intervention-engine.ts
  - lib/db/sentiment-log.ts
  - lib/feedback/sentiment.ts
  - app/api/fred/chat/route.ts
  - lib/sentiment/__tests__/stress-detector.test.ts
  - lib/sentiment/__tests__/intervention-engine.test.ts
  - supabase/migrations/20260308_sentiment_signals.sql
autonomous: true

must_haves:
  truths:
    - "Every chat message produces a sentiment signal logged to sentiment_signals table"
    - "Stress pattern detection runs before LLM call and injects intervention into system prompt when threshold met"
    - "Intervention cooldown prevents FRED from spamming wellbeing check-ins"
    - "Sentiment extraction never blocks or delays the user-facing response"
  artifacts:
    - path: "lib/sentiment/stress-detector.ts"
      provides: "Rolling window stress pattern detection"
      exports: ["detectStressPattern", "shouldIntervene", "extractTopicsFromMessage"]
    - path: "lib/sentiment/intervention-engine.ts"
      provides: "Contextual intervention prompt generation"
      exports: ["generateIntervention", "buildInterventionBlock"]
    - path: "lib/db/sentiment-log.ts"
      provides: "Sentiment signal CRUD"
      exports: ["logSentimentSignal", "getRecentSentimentSignals", "wasInterventionTriggeredRecently"]
    - path: "lib/feedback/sentiment.ts"
      provides: "Per-message sentiment extraction (LLM + heuristic)"
      exports: ["extractSentiment", "extractSentimentFromText"]
  key_links:
    - from: "app/api/fred/chat/route.ts"
      to: "lib/sentiment/stress-detector.ts"
      via: "detectStressPattern() called before LLM, result injected into system prompt"
      pattern: "detectStressPattern"
    - from: "app/api/fred/chat/route.ts"
      to: "lib/db/sentiment-log.ts"
      via: "logSentimentSignal() fire-and-forget after extraction"
      pattern: "logSentimentSignal"
    - from: "lib/sentiment/stress-detector.ts"
      to: "lib/db/sentiment-log.ts"
      via: "getRecentSentimentSignals() for rolling window"
      pattern: "getRecentSentimentSignals"
---

<objective>
Verify and harden the sentiment extraction pipeline and stress detection system that is already partially wired in the chat route.

Purpose: Phase 83 requires FRED to extract sentiment from every message, detect stress patterns, and proactively intervene. The core modules already exist from prior work (lib/sentiment/*, lib/feedback/sentiment.ts, lib/db/sentiment-log.ts) and are imported in the chat route. This plan verifies the wiring is complete, adds missing test coverage, and ensures the stress_level field is correctly persisted for the admin dashboard (Plan 02).

Output: Fully verified sentiment pipeline with comprehensive tests, correct DB persistence, and hardened intervention logic.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@lib/sentiment/stress-detector.ts
@lib/sentiment/intervention-engine.ts
@lib/db/sentiment-log.ts
@lib/feedback/sentiment.ts
@lib/feedback/sentiment-aggregator.ts
@lib/fred/actors/burnout-detector.ts
@app/api/fred/chat/route.ts
@lib/sentiment/__tests__/stress-detector.test.ts
@supabase/migrations/20260308_sentiment_signals.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and harden the sentiment pipeline wiring in the chat route</name>
  <files>
    app/api/fred/chat/route.ts
    lib/db/sentiment-log.ts
    lib/sentiment/stress-detector.ts
    supabase/migrations/20260308_sentiment_signals.sql
  </files>
  <action>
    The chat route already imports and calls the Phase 83 sentiment modules (lines 49-51, 671-704). Verify the following wiring is correct and complete:

    1. **Pre-LLM stress detection (lines 671-704):** Confirm `detectStressPattern()` is called with `extractSentimentFromText()` (heuristic, sync) BEFORE the LLM call. The result should produce a `wellbeingBlock` string that gets injected into the system prompt. Verify the `wellbeingBlock` is included in the prompt assembly (around line 760).

    2. **Post-LLM sentiment logging (lines 893-920):** Confirm `extractSentiment()` (LLM-based) runs fire-and-forget AFTER the response is generated, and its result is stored via `insertFeedbackSignal()`. This is the detailed sentiment — separate from the pre-LLM heuristic used for stress detection.

    3. **Stress level persistence:** In the `logSentimentSignal()` call (lines 678-688), verify `stress_level` is set to `stressSignal.score` (the computed weighted score, 0-1 range). This value is critical for the admin dashboard aggregation in Plan 02.

    4. **Intervention cooldown:** Verify that when `shouldIntervene()` returns true, a SECOND `logSentimentSignal()` is called with `intervention_triggered: true` (lines 694-703). This prevents duplicate interventions via the `wasInterventionTriggeredRecently()` cooldown check.

    5. **Migration completeness:** Check `supabase/migrations/20260308_sentiment_signals.sql` has an index on `(user_id, created_at DESC)` for efficient rolling window queries. Add a service-role SELECT policy if missing (admin dashboard needs service-role reads).

    If any of the above is not wired correctly, fix it. If already correct, move to the next task.
  </action>
  <verify>
    - `grep -n "detectStressPattern\|logSentimentSignal\|shouldIntervene\|wellbeingBlock" app/api/fred/chat/route.ts` shows all 4 are present and wired
    - `grep -n "stress_level" lib/db/sentiment-log.ts` shows the field is persisted
    - The migration file has an index on `(user_id, created_at DESC)`
    - `npm run build` compiles without errors
  </verify>
  <done>
    The sentiment pipeline is fully wired: every chat message gets heuristic sentiment pre-LLM (for stress detection) and LLM sentiment post-response (for logging). Stress signals are persisted with score, topics, and intervention_triggered flag. Intervention cooldown prevents spam.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add comprehensive tests for intervention engine and pipeline integration</name>
  <files>
    lib/sentiment/__tests__/intervention-engine.test.ts
    lib/sentiment/__tests__/stress-detector.test.ts
  </files>
  <action>
    Tests already exist for stress-detector (lib/sentiment/__tests__/stress-detector.test.ts). Add a new test file for the intervention engine and extend existing tests:

    1. **Create `lib/sentiment/__tests__/intervention-engine.test.ts`:**
       - Test `generateIntervention()` with critical level: should mention wellbeing check-in and /dashboard/wellbeing
       - Test `generateIntervention()` with high level: should mention stepping back
       - Test both include the founder's name when provided
       - Test both include topic string when topics are present
       - Test `buildInterventionBlock()` wraps text in `[FOUNDER WELLBEING CONTEXT]` tags
       - Test naturalness guard: output should NOT contain "stress level" or "monitoring system"

    2. **Extend stress-detector tests (if not already covered):**
       - Test `extractTopicsFromMessage()` with personal topics ("my family is struggling") -> should detect "personal"
       - Test `extractTopicsFromMessage()` with competition ("we're losing to our competitor") -> should detect "competition"
       - Test edge case: empty message returns empty topics
       - Test edge case: very long message with multiple topic hits caps at reasonable count

    Use vitest with `vi.mock` for DB dependencies. Follow the existing test patterns in the file.
  </action>
  <verify>
    - `npx vitest run lib/sentiment/__tests__/intervention-engine.test.ts` passes
    - `npx vitest run lib/sentiment/__tests__/stress-detector.test.ts` passes
    - All new tests are meaningful (not trivially true)
  </verify>
  <done>
    Intervention engine has full test coverage. Stress detector topic extraction has edge case coverage. All tests pass.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds without errors
- `npx vitest run lib/sentiment/` passes all tests
- `grep -c "logSentimentSignal" app/api/fred/chat/route.ts` shows at least 2 calls (normal + intervention)
- Chat route compiles with all sentiment imports resolved
</verification>

<success_criteria>
- Every chat message produces a sentiment signal in the sentiment_signals table with label, confidence, stress_level, and topics
- Stress pattern detection runs pre-LLM and injects intervention text when threshold is met
- Intervention cooldown prevents back-to-back interventions within 3 messages
- Comprehensive test suite validates detection logic, intervention generation, and edge cases
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/83/83-01-SUMMARY.md`
</output>
