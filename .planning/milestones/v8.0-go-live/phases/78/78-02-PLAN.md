---
phase: 78-oases-gating
plan: 02
type: execute
wave: 1
depends_on: ["78-01"]
files_modified:
  - components/tier/feature-lock.tsx
  - app/dashboard/boardy/page.tsx
  - components/boardy/journey-celebration.tsx
  - components/journey/journey-gate.tsx
  - app/dashboard/layout.tsx
  - components/mobile/mobile-bottom-nav.tsx
  - components/mobile/mobile-home.tsx
  - components/oases/oases-visualizer.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Users cannot access tools or content gated behind a later stage"
    - "Fund matching (Boardy) is visually grayed out with completion percentage messaging until 100%"
    - "All UI surfaces say 'Progress' instead of 'Journey'"
    - "'Chat with Fred' entry point is prominent and persistently accessible"
    - "'Open Roadmap' button is visible and persistently accessible"
    - "Four main sections (Home, Mentor, Progress, Profile) persistently visible on mobile and desktop"
  artifacts:
    - path: "components/tier/feature-lock.tsx"
      provides: "Stage-aware gating with journey percentage overlay"
      contains: "requiredStage"
    - path: "app/dashboard/boardy/page.tsx"
      provides: "Boardy page with journey gate at 100%"
      contains: "JourneyGate"
    - path: "components/journey/journey-gate.tsx"
      provides: "Journey completion gate with 'Complete your venture journey first (You're at X%)' messaging"
      contains: "Complete.*venture journey"
    - path: "components/mobile/mobile-bottom-nav.tsx"
      provides: "Mobile nav with 4 persistent sections"
      contains: "Home.*Mentor.*Progress.*Profile"
    - path: "app/dashboard/layout.tsx"
      provides: "Desktop sidebar with 'Progress' label and 'Chat with Fred' entry"
      contains: "Progress"
  key_links:
    - from: "app/dashboard/boardy/page.tsx"
      to: "components/journey/journey-gate.tsx"
      via: "JourneyGate wrapper component"
      pattern: "JourneyGate"
    - from: "components/tier/feature-lock.tsx"
      to: "lib/oases/stage-config.ts"
      via: "stage index comparison"
      pattern: "getStageIndex|isRouteGated"
    - from: "components/mobile/mobile-bottom-nav.tsx"
      to: "/dashboard/journey"
      via: "Progress nav link"
      pattern: "Progress.*journey"
---

<objective>
Enforce stage gating across the UI, lock fund matching until 100% journey completion, update all UI labels, and ensure persistent navigation meets UILABEL requirements.

Purpose: With the Oases visualization and tracking in place (Plan 01), this plan enforces the stage gating at the UI level, ensures Boardy is locked with clear messaging, renames remaining "Journey" references to "Progress", and configures the four main sections for mobile and desktop persistent navigation.

Output: A fully gated UI where users cannot skip ahead, Boardy shows locked state with completion percentage, and navigation surfaces are consistent with UILABEL-02 through UILABEL-05.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/REQUIREMENTS.md
@components/tier/feature-lock.tsx
@components/journey/journey-gate.tsx
@app/dashboard/boardy/page.tsx
@app/dashboard/layout.tsx
@components/mobile/mobile-bottom-nav.tsx
@components/mobile/mobile-home.tsx
@lib/oases/stage-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance stage gating enforcement and Boardy fund matching lock</name>
  <files>
    components/tier/feature-lock.tsx
    components/journey/journey-gate.tsx
    app/dashboard/boardy/page.tsx
    components/boardy/journey-celebration.tsx
  </files>
  <action>
    The existing FeatureLock and JourneyGate components already support stage-based gating. Enhance them for v8.0:

    1. **JourneyGate enhancement** (`components/journey/journey-gate.tsx`):
       - Update the locked state messaging to match the exact v8.0 requirement: "Complete your venture journey first (You're at X%)" where X is the current journey percentage.
       - Keep the existing stage badges and progress bar.
       - Add a "Chat with Fred to continue" CTA button linking to /chat alongside the existing "Continue Journey" button.

    2. **FeatureLock stage-gating** (`components/tier/feature-lock.tsx`):
       - Verify the stage-blocked overlay shows when a user tries to access a stage-gated feature. It should display: which stage is required, how many steps remain, and a link to the dashboard.
       - Ensure stage-blocked takes priority over tier-blocked (already implemented — verify it still works).

    3. **Boardy fund matching lock** (`app/dashboard/boardy/page.tsx`):
       - Verify the existing JourneyGate wraps the Boardy content.
       - Ensure the grayed-out state shows the exact messaging: "Complete your venture journey first (You're at X%)"
       - The `requiredPercent` should be 100 (default) — verify this.
       - When unlocked (100%), the JourneyCelebration component should render (already exists in `components/boardy/journey-celebration.tsx` — verify it triggers).

    4. **Stage gating on remaining routes**: Verify that all routes defined in STAGE_CONFIG.gatedRoutes are actually gated. Check that:
       - `/dashboard/positioning`, `/dashboard/insights`, `/dashboard/coaching`, `/dashboard/next-steps` require Validation stage
       - `/dashboard/strategy`, `/dashboard/documents`, `/dashboard/pitch-deck`, `/dashboard/readiness` require Build stage
       - `/dashboard/investor-lens`, `/dashboard/investor-targeting`, `/dashboard/investor-readiness`, `/dashboard/investor-evaluation` require Launch stage
       - `/dashboard/boardy`, `/dashboard/agents`, `/dashboard/communities`, `/dashboard/marketplace` require Grow stage

       NOTE: Stage gating is currently enforced via the sidebar navigation visibility (conditionalNavItems in layout.tsx) and FeatureLock on individual pages. The route-level gating via STAGE_CONFIG.gatedRoutes exists but may not be wired into middleware. For v8.0, ensure at minimum the FeatureLock component is used on each gated page. Do NOT add middleware-level route blocking in this task — that belongs to Phase 80 (Structured Stage-Gate Enforcement).

    AVOID: Do not modify the OasesProgress type or progress API. Do not add middleware-level route blocking (that's Phase 80). Focus on UI-level enforcement only.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no new errors
    - Boardy page shows JourneyGate lock with "Complete your venture journey first (You're at X%)"
    - FeatureLock with requiredStage shows stage-blocked overlay
    - JourneyCelebration component exists and renders conditionally when journey is 100%
  </verify>
  <done>
    Stage gating enforced at UI level on all gated routes. Boardy shows locked state with completion percentage. JourneyGate messaging matches v8.0 requirement. FeatureLock stage-blocking takes priority over tier-blocking.
  </done>
</task>

<task type="auto">
  <name>Task 2: UI relabeling ("Progress"), persistent navigation, and "Chat with Fred" / "Open Roadmap" entry points</name>
  <files>
    app/dashboard/layout.tsx
    components/mobile/mobile-bottom-nav.tsx
    components/mobile/mobile-home.tsx
    components/oases/oases-visualizer.tsx
  </files>
  <action>
    Ensure all UILABEL requirements are met across mobile and desktop:

    1. **UILABEL-02: "Journey" to "Progress" rename**:
       - In `app/dashboard/layout.tsx`: The sidebar nav already shows "Progress" for the journey link (line 98-99, `name: "Progress"`, `href: "/dashboard/journey"`). Verify no other references to "Journey" remain in user-facing nav text. The page title "Your Progress" on `/dashboard/journey/page.tsx` is already correct.
       - Search all user-facing strings for "Journey" and replace with "Progress" where it refers to the nav section. NOTE: Internal variable names, file paths, and the Oases concept "Venture Journey" should NOT be renamed — only the nav label.

    2. **UILABEL-03: Prominent "Chat with Fred" entry point**:
       - The OasesVisualizer already has a "Chat with Fred" button. Verify it is visually prominent (full-width orange button, not a subtle link).
       - In `app/dashboard/layout.tsx`: The sidebar already has an "Ask Fred for Help" button at the bottom. Rename it to "Chat with Fred" to match the requirement and make it more prominent — use the orange brand color bg-[#ff6a1a] with white text instead of the current ghost style.
       - In `components/mobile/mobile-bottom-nav.tsx`: The "Mentor" tab links to /chat — this serves as the mobile "Chat with Fred" entry point. Verify it's visible.

    3. **UILABEL-04: "Open Roadmap" button**:
       - The StageDetailModal already has an "Open Roadmap" link. Ensure it's a proper Button (not just a text link).
       - Add an "Open Roadmap" quick-action link to the OasesVisualizer header area (next to the progress ring), so users can jump to `/dashboard/journey` from the dashboard without opening a modal.

    4. **UILABEL-05: Four main sections persistently visible**:
       - The requirement says 4 main sections on mobile + desktop: Home, Mentor (Chat), Progress, Profile.
       - In `components/mobile/mobile-bottom-nav.tsx`: Currently has 7 items (Home, Mentor, Next, Voice, Progress, Community, Profile). Simplify to 4 primary items: Home, Mentor, Progress (link to /dashboard/journey), Profile — plus the center Voice button (5 total with voice as the special center).
       - Remove "Next" and "Community" from the bottom nav (they're accessible from sidebar and dashboard).
       - Ensure "Progress" links to `/dashboard/journey` (the Oases roadmap page).
       - On desktop sidebar in `app/dashboard/layout.tsx`: The 4 sections are already visible as top nav items (Home, Mentor, Progress, Settings). No changes needed.

    NOTE: `app/dashboard/layout.tsx` has pre-commit hooks that auto-revert changes. The workaround is to create adapter patterns or verify the changes are compatible. If pre-commit revert happens, check if the changes conflict with the hook's expected content and adjust.

    AVOID: Do not rename internal code variables or file paths (keep "journey" in paths). Only rename user-facing display text. Do not change the FloatingChatWidget — it already provides chat access.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no new errors
    - `npm run build` succeeds
    - Desktop sidebar shows "Progress" (not "Journey") for the roadmap link
    - Sidebar "Chat with Fred" button uses orange brand color
    - Mobile bottom nav shows exactly: Home, Mentor, [Voice], Progress, Profile
    - "Open Roadmap" button visible in OasesVisualizer header
    - All 4 main sections visible without scrolling on mobile
  </verify>
  <done>
    All UILABEL requirements met: "Progress" label everywhere, "Chat with Fred" is prominent on desktop and mobile, "Open Roadmap" accessible from dashboard, 4 main sections persistently visible on all viewports.
  </done>
</task>

</tasks>

<verification>
- JOURNEY-03 (UI level): Users see locked state on gated features with clear messaging about what to complete
- JOURNEY-05: Boardy grayed out with "Complete your venture journey first (You're at X%)" messaging
- UILABEL-02: All nav surfaces show "Progress" instead of "Journey"
- UILABEL-03: "Chat with Fred" is a prominent, visible entry point on dashboard, sidebar, and mobile
- UILABEL-04: "Open Roadmap" button visible on dashboard and in stage detail modal
- UILABEL-05: Home, Mentor, Progress, Profile visible as persistent sections on mobile and desktop
- `npm run build` succeeds
- `npx tsc --noEmit` passes
</verification>

<success_criteria>
1. Boardy page shows journey gate lock with accurate percentage and messaging
2. All gated routes show FeatureLock overlay when user hasn't reached required stage
3. All nav surfaces say "Progress" not "Journey"
4. "Chat with Fred" button is orange and prominent in sidebar
5. Mobile bottom nav has exactly 4 sections + voice (Home, Mentor, Voice, Progress, Profile)
6. "Open Roadmap" button accessible from dashboard OasesVisualizer
7. Build and type check pass
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/78/78-02-SUMMARY.md`
</output>
