---
phase: 86-fred-response-conciseness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/fred/voice.ts
  - lib/agents/fred-agent-voice.ts
  - lib/ai/__tests__/conciseness.test.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "FRED default chat responses are 2-3 sentences max with follow-up offer"
    - "Action items prescribe 1-week micro-steps, never multi-month asks"
    - "Voice (TTS) responses sound natural and concise (1-2 sentences)"
    - "All prompt assembly paths include conciseness and baby-step rules"
  artifacts:
    - path: "lib/ai/prompt-layers.ts"
      provides: "CONCISENESS PROTOCOL and BABY-STEP COACHING sections in core prompt"
      contains: "CONCISENESS PROTOCOL"
    - path: "lib/fred/voice.ts"
      provides: "Voice preamble with conciseness + baby-step constraints"
      contains: "baby-step"
    - path: "lib/agents/fred-agent-voice.ts"
      provides: "Agent voice constant with conciseness rules"
      contains: "2-3 sentences"
    - path: "lib/ai/__tests__/conciseness.test.ts"
      provides: "Tests verifying conciseness rules in all prompt paths"
      min_lines: 40
  key_links:
    - from: "lib/ai/prompt-layers.ts"
      to: "lib/fred/voice.ts"
      via: "shared conciseness rules"
      pattern: "CONCISENESS|baby.step"
    - from: "lib/ai/prompt-layers.ts"
      to: "lib/ai/prompts.ts"
      via: "COACHING_PROMPTS topic overlays include conciseness reminder"
      pattern: "Keep initial responses to 2-3 sentences"
---

<objective>
Reinforce FRED response conciseness and baby-stepping across all prompt paths: chat, voice, and agent tools.

Purpose: The core prompt already contains CONCISENESS PROTOCOL (lines 204-218) and BABY-STEP COACHING (lines 219-235) sections. The COACHING_PROMPTS overlays already include "Keep initial responses to 2-3 sentences" reminders. This plan strengthens the voice preamble and agent voice constant to match, and adds test coverage to prevent regression.

Output: Updated voice.ts and fred-agent-voice.ts with conciseness/baby-step rules; new test file verifying all prompt assembly paths include these rules.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@lib/ai/prompt-layers.ts
@lib/ai/prompts.ts
@lib/fred/voice.ts
@lib/agents/fred-agent-voice.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add conciseness and baby-step rules to voice preamble and agent voice constant</name>
  <files>lib/fred/voice.ts, lib/agents/fred-agent-voice.ts</files>
  <action>
    **lib/fred/voice.ts** — The `buildFredVoicePreamble()` function already has a voice-specific conciseness line ("In voice mode, keep responses even shorter -- 1-2 sentences max..."). Add a baby-step rule for voice mode:

    After the existing voice conciseness line (line 73-75), add:
    ```
    "When suggesting actions, give ONE thing to do this week. Never list multi-step plans in voice -- founders can't write them down. Say: 'This week, focus on X. Check back and tell me what you learn.'"
    ```

    **lib/agents/fred-agent-voice.ts** — The `FRED_AGENT_VOICE` constant currently has voice/style but no conciseness or baby-step rules. Add after the "What you never do" section:

    ```
    Conciseness: Keep initial responses to 2-3 sentences. Lead with the most important point. Offer depth as a follow-up: "Want me to break that down?"

    Baby-stepping: When giving action items, prescribe 1-week micro-steps only. Never give multi-month plans. Frame as "This week, do X" not "Over the next quarter, build Y."
    ```

    Do NOT modify lib/ai/prompt-layers.ts — the core prompt already has comprehensive CONCISENESS PROTOCOL and BABY-STEP COACHING sections (lines 204-235). Do NOT modify lib/ai/prompts.ts — the COACHING_PROMPTS overlays already have conciseness reminders.
  </action>
  <verify>
    Run: `grep -n "baby-step\|baby.step\|1-week\|micro-step" lib/fred/voice.ts lib/agents/fred-agent-voice.ts` — both files should have matches.
    Run: `grep -n "2-3 sentences" lib/agents/fred-agent-voice.ts` — should have a match.
    Run: `npx tsc --noEmit --pretty 2>&1 | head -20` — no new type errors.
  </verify>
  <done>
    Voice preamble includes baby-step rule for voice mode. Agent voice constant includes both conciseness (2-3 sentences) and baby-step (1-week micro-steps) rules. No type errors introduced.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add conciseness regression tests across all prompt assembly paths</name>
  <files>lib/ai/__tests__/conciseness.test.ts</files>
  <action>
    Create a new test file `lib/ai/__tests__/conciseness.test.ts` that verifies conciseness and baby-stepping rules are present in all prompt assembly paths. Use Vitest. Tests should cover:

    1. **Core prompt contains CONCISENESS PROTOCOL** — Import `FRED_CORE_PROMPT` from `@/lib/ai/prompt-layers`, assert `.content` contains "CONCISENESS PROTOCOL", "2-3 sentences maximum", and "non-negotiable".

    2. **Core prompt contains BABY-STEP COACHING** — Assert `FRED_CORE_PROMPT.content` contains "BABY-STEP COACHING", "1-week micro-steps", "Maximum time horizon for any single action item: 7 days".

    3. **All COACHING_PROMPTS include conciseness reminder** — Import `COACHING_PROMPTS` from `@/lib/ai/prompts`, iterate over all keys, assert each value contains "Keep initial responses to 2-3 sentences".

    4. **buildSystemPrompt output includes conciseness** — Import `buildSystemPrompt` from `@/lib/ai/prompts`, call with empty string, assert output contains "CONCISENESS PROTOCOL" and "BABY-STEP COACHING".

    5. **Voice preamble includes conciseness** — Import `buildFredVoicePreamble` from `@/lib/fred/voice`, call with defaults, assert output contains "1-2 sentences" and a baby-step related phrase.

    6. **FRED_AGENT_VOICE includes conciseness** — Import `FRED_AGENT_VOICE` from `@/lib/agents/fred-agent-voice`, assert it contains "2-3 sentences" and "1-week".

    7. **Anti-patterns NOT present in core prompt** — Assert `FRED_CORE_PROMPT.content` does NOT contain "over the next 3 months", "over the next quarter", or "Phase 1: ... Phase 2: ... Phase 3:".

    Keep tests descriptive with `describe("FRED Conciseness & Baby-Stepping")` grouping.
  </action>
  <verify>
    Run: `npx vitest run lib/ai/__tests__/conciseness.test.ts --reporter=verbose 2>&1 | tail -30` — all tests pass.
  </verify>
  <done>
    All 7 test cases pass. Conciseness and baby-stepping rules are verified across core prompt, coaching overlays, voice preamble, and agent voice constant. Any future prompt change that removes these rules will cause test failure.
  </done>
</task>

</tasks>

<verification>
1. `grep -c "CONCISENESS PROTOCOL" lib/ai/prompt-layers.ts` returns 1 (unchanged)
2. `grep -c "BABY-STEP COACHING" lib/ai/prompt-layers.ts` returns 1 (unchanged)
3. `grep -c "2-3 sentences" lib/ai/prompts.ts` returns 5 (one per COACHING_PROMPTS topic, unchanged)
4. `grep "baby" lib/fred/voice.ts` returns match (new)
5. `grep "2-3 sentences" lib/agents/fred-agent-voice.ts` returns match (new)
6. `npx vitest run lib/ai/__tests__/conciseness.test.ts` — all tests pass (new)
7. `npx tsc --noEmit` — no type errors
</verification>

<success_criteria>
- FRED core prompt CONCISENESS PROTOCOL and BABY-STEP COACHING sections unchanged and verified by tests
- Voice preamble includes baby-step rule for voice-mode action items
- FRED_AGENT_VOICE constant includes conciseness (2-3 sentences) and baby-step (1-week) rules
- 7 regression tests pass covering all prompt assembly paths
- No type errors, no existing test failures
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/86/86-01-SUMMARY.md`
</output>
