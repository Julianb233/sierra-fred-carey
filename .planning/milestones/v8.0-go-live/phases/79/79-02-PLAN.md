---
phase: 79-active-founder-memory-layer
plan: 02
type: execute
wave: 1
depends_on: ["79-01"]
files_modified:
  - lib/fred/active-memory.ts
  - lib/ai/prompts.ts
  - lib/fred/context-builder.ts
  - app/get-started/page.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Every FRED chat response references at least one founder-specific detail (company name, market, stage, challenge)"
    - "When memory data is stale or missing, FRED asks a clarifying question rather than guessing or giving generic advice"
    - "Co-founder field is captured during onboarding and available to FRED in all subsequent interactions"
  artifacts:
    - path: "lib/fred/active-memory.ts"
      provides: "formatMemoryBlock with CRITICAL INSTRUCTION enforcing per-response context referencing"
      contains: "CRITICAL INSTRUCTION"
    - path: "lib/ai/prompts.ts"
      provides: "buildSystemPrompt injects founder context with memory-aware instructions"
      contains: "FOUNDER_CONTEXT"
    - path: "lib/fred/context-builder.ts"
      provides: "buildFounderContextWithFacts assembles full context including active memory"
      contains: "formatMemoryBlock"
  key_links:
    - from: "lib/fred/context-builder.ts"
      to: "lib/fred/active-memory.ts"
      via: "formatMemoryBlock call to build ACTIVE FOUNDER CONTEXT section"
      pattern: "formatMemoryBlock"
    - from: "lib/ai/prompts.ts"
      to: "lib/fred/context-builder.ts"
      via: "{{FOUNDER_CONTEXT}} placeholder replaced with context block"
      pattern: "FOUNDER_CONTEXT"
    - from: "app/api/fred/chat/route.ts"
      to: "lib/fred/context-builder.ts"
      via: "buildFounderContextWithFacts called to assemble prompt context"
      pattern: "buildFounderContextWithFacts"
---

<objective>
Ensure FRED's system prompt always injects founder-specific context and enforces personalized responses. Add stale data handling (ask clarifying questions) and co-founder capture during onboarding.

Purpose: Success criteria 1 (every response references founder-specific detail), success criteria 3 (co-founder captured in onboarding), and success criteria 4 (stale/missing data triggers clarifying questions instead of generic advice). The existing formatMemoryBlock already has a CRITICAL INSTRUCTION block and stale/missing field handling. This plan strengthens the enforcement, adds co-founder to the onboarding flow, and ensures the prompt architecture guarantees personalization.

Output: Hardened prompt injection ensuring zero generic FRED responses, co-founder field in onboarding, and robust stale data handling.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/REQUIREMENTS.md
@.planning/milestones/v8.0-go-live/phases/79/79-01-PLAN.md
@lib/fred/active-memory.ts
@lib/ai/prompts.ts
@lib/ai/prompt-layers.ts
@lib/fred/context-builder.ts
@app/get-started/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strengthen FRED prompt enforcement for personalized responses and stale data handling</name>
  <files>
    lib/fred/active-memory.ts
    lib/ai/prompt-layers.ts
  </files>
  <action>
1. In `lib/fred/active-memory.ts`, enhance `formatMemoryBlock`:
   - Strengthen the existing CRITICAL INSTRUCTION block to be more specific about HOW to reference context. Replace the current instruction with:
     ```
     CRITICAL INSTRUCTION: You MUST reference at least one founder-specific detail in EVERY response.
     Rules:
     1. ALWAYS address the founder by name if known: "Hey [name]..." or "[name], here's what I think..."
     2. ALWAYS tie advice to their specific context. Instead of "startups should focus on..." say "Since [company] is in the [market] space at [stage]..."
     3. NEVER give advice that could apply to any founder. Every recommendation must reference their specific company, market, challenge, stage, or goals.
     4. If you catch yourself writing a generic sentence, rewrite it with their context.
     Examples of BAD (generic): "You should focus on product-market fit" / "Most startups struggle with this"
     Examples of GOOD (personalized): "For [company] in [market], product-market fit means..." / "Given your challenge with [biggest_challenge]..."
     ```

   - Enhance the stale field handling to be more actionable. Update the stale fields section:
     ```
     **Stale Context (needs re-confirmation):** The following details are over 7 days old and may be outdated.
     DO NOT assume they are still accurate. Instead, naturally verify them:
     [list stale fields with their old values]
     Example: "Last time we spoke, you mentioned [old value]. Is that still where things stand, or has anything changed?"
     RULE: If a stale field is directly relevant to the founder's current question, ASK before advising. Do not give advice based on potentially outdated information.
     ```

   - Enhance the missing field handling to prioritize asking over guessing:
     ```
     **Missing Context:** I don't have data for: [fields].
     RULE: Do NOT guess or assume values for missing fields. Instead:
     - If the missing field is relevant to the current conversation, ask about it directly
     - Weave questions naturally: "By the way, I don't think you've told me about [field] yet. What's the situation there?"
     - Collect at most 2 missing fields per exchange to avoid feeling like an interrogation
     - NEVER fabricate or assume company names, market segments, or challenges
     ```

2. In `lib/ai/prompt-layers.ts`, verify the `{{FOUNDER_CONTEXT}}` placeholder is present in FRED_CORE_PROMPT.content:
   - The placeholder should exist in the prompt template. Grep for it.
   - If present, no changes needed to prompt-layers.ts.
   - If missing, add `\n\n{{FOUNDER_CONTEXT}}\n\n` after the communication style section and before the closing of the core prompt.

  IMPORTANT: Do NOT modify the core prompt content (voice, identity, methodology). Only verify/ensure the {{FOUNDER_CONTEXT}} injection point exists. The formatMemoryBlock changes are the primary work here.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - Grep `CRITICAL INSTRUCTION` in active-memory.ts shows the strengthened instruction
    - Grep `Do NOT guess` in active-memory.ts shows missing field handling
    - Grep `FOUNDER_CONTEXT` in prompt-layers.ts confirms placeholder exists
    - `npm run test -- --run lib/ai/__tests__/prompts.test.ts` passes
  </verify>
  <done>
    - formatMemoryBlock produces an ACTIVE FOUNDER CONTEXT section that:
      - Mandates per-response founder detail referencing with specific examples
      - Instructs FRED to ask before advising when fields are stale
      - Prevents guessing/fabrication for missing fields
    - {{FOUNDER_CONTEXT}} injection point confirmed in FRED_CORE_PROMPT
  </done>
</task>

<task type="auto">
  <name>Task 2: Add co-founder capture to onboarding and verify end-to-end context flow</name>
  <files>
    app/get-started/page.tsx
    lib/fred/context-builder.ts
  </files>
  <action>
1. In `app/get-started/page.tsx` (the onboarding flow):
   - Find the existing onboarding form/questionnaire fields
   - Add a "Co-Founder" field to the onboarding questions. This should be:
     - A text input field (not dropdown) with placeholder: "Co-founder name(s), or 'Solo founder'"
     - Positioned after the company name / stage questions
     - Optional (not required) -- founders can skip if solo
     - The field value should be saved to the `profiles` table `co_founder` column on form submission
   - If the onboarding flow uses a multi-step wizard, add the co-founder field to the same step as company details
   - If the form submission handler already writes to profiles, add `co_founder` to the update payload
   - Match existing styling patterns (Tailwind classes, input component styles from the file)

   IMPORTANT: The `co_founder` column already exists in profiles (migration `20260308100001_add_co_founder_and_company_name.sql`). No schema changes needed.

   NOTE: The `app/get-started/page.tsx` file may have pre-commit hook protections. If so, check if the onboarding flow has alternative entry points (e.g., `app/(onboarding)/` or `components/onboarding/`). If the file is protected by hooks, create the co-founder field in the component that the get-started page renders, not in the page file itself.

2. In `lib/fred/context-builder.ts`, verify the end-to-end context assembly flow:
   - `buildFounderContextWithFacts` already calls `buildActiveFounderMemory` then `formatMemoryBlock`
   - Verify the result is injected into the system prompt via `buildSystemPrompt(founderContext)` in `lib/ai/prompts.ts`
   - Verify the chat route calls `buildFounderContextWithFacts` and passes the result to the prompt builder
   - No code changes expected here if the wiring is already correct. This is a verification step.
   - If any wiring is missing, add the minimal connection needed.

3. Test the full flow mentally:
   - User signs up -> onboarding captures co_founder -> saved to profiles.co_founder
   - User opens chat -> buildFounderContextWithFacts loads profile -> buildActiveFounderMemory populates co_founder field
   - formatMemoryBlock outputs "**Co-Founder:** Sarah Chen" in ACTIVE FOUNDER CONTEXT
   - buildSystemPrompt replaces {{FOUNDER_CONTEXT}} with the context block
   - FRED sees the co-founder in its system prompt and references it naturally
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds
    - Grep `co_founder` or `co-founder` in get-started/page.tsx (or onboarding component) shows the new field
    - Grep `formatMemoryBlock` in context-builder.ts confirms it's called in the context assembly
    - Grep `buildFounderContextWithFacts` in route.ts confirms it's called in the chat pipeline
    - The onboarding form includes a co-founder text input that saves to profiles.co_founder
  </verify>
  <done>
    - Co-founder field exists in the onboarding flow and persists to profiles.co_founder on submission
    - The full context pipeline is verified: profile -> active memory -> formatMemoryBlock -> {{FOUNDER_CONTEXT}} -> system prompt
    - Every FRED response has access to co-founder data (when captured) in its system prompt
    - Stale/missing co-founder data triggers natural clarifying questions per the enhanced formatMemoryBlock rules
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. `npm run test -- --run` passes all existing tests
4. Onboarding flow includes co-founder capture field
5. formatMemoryBlock output includes strengthened personalization enforcement
6. Stale fields trigger "ask before advising" behavior in the prompt
7. Missing fields trigger "do not guess" behavior in the prompt
</verification>

<success_criteria>
- Success Criterion 1 satisfied: formatMemoryBlock CRITICAL INSTRUCTION mandates founder-specific detail in every response with concrete examples of good vs bad
- Success Criterion 3 satisfied: Co-founder field captured during onboarding, stored in profiles.co_founder, loaded into active memory, injected into FRED's system prompt
- Success Criterion 4 satisfied: Stale fields trigger re-confirmation prompts ("Is that still the case?"); missing fields trigger clarifying questions ("I don't have data for X -- what's the situation?"); FRED never guesses or fabricates missing context
- MEMORY-02 partially satisfied (alongside 79-01): The prompt enforcement ensures FRED naturally restates founder context in responses, creating a "capture and restate" loop
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/79/79-02-SUMMARY.md`
</output>
