---
phase: 79-active-founder-memory-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/fred/founder-memory-types.ts
  - lib/fred/active-memory.ts
  - lib/ai/memory-extraction-prompt.ts
  - lib/fred/enrichment/extractor.ts
  - app/api/fred/chat/route.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Founder context is auto-refreshed after each conversation -- new information mentioned in chat updates the memory layer"
    - "FRED captures and stores structured memory from chat data including problem statements, traction, and key decisions"
    - "FRED rephrases user inputs and stores them for later reference"
  artifacts:
    - path: "lib/fred/founder-memory-types.ts"
      provides: "Extended core memory fields with traction, revenue, decisions, goals"
      contains: "traction"
    - path: "lib/fred/active-memory.ts"
      provides: "Enhanced memory persistence pipeline with auto-refresh and rephrase storage"
      contains: "persistMemoryUpdates"
    - path: "lib/ai/memory-extraction-prompt.ts"
      provides: "Enhanced extraction prompt covering all structured memory fields"
      contains: "traction"
    - path: "app/api/fred/chat/route.ts"
      provides: "Chat route with post-response memory auto-refresh"
      contains: "extractMemoryUpdates"
  key_links:
    - from: "app/api/fred/chat/route.ts"
      to: "lib/fred/active-memory.ts"
      via: "extractMemoryUpdates + persistMemoryUpdates fire-and-forget after streaming"
      pattern: "extractMemoryUpdates.*persistMemoryUpdates"
    - from: "lib/ai/memory-extraction-prompt.ts"
      to: "lib/fred/founder-memory-types.ts"
      via: "CORE_MEMORY_FIELDS import for valid field names"
      pattern: "CORE_MEMORY_FIELDS"
---

<objective>
Upgrade the structured founder memory schema and extraction pipeline so FRED captures richer context from every conversation and auto-refreshes the memory layer after each exchange.

Purpose: MEMORY-01 (per-user mini-brain with structured memory for problem, traction, decisions) and MEMORY-02 (capture and restate -- rephrase inputs, store for reference, auto-refresh after conversations). The existing Phase 79 implementation from v3.0 established the foundation (FounderMemory types, buildActiveFounderMemory, extractMemoryUpdates, persistMemoryUpdates). This plan extends it to capture the full set of structured fields Fred Cary needs to give personalized advice: traction metrics, revenue status, decisions made, 90-day goals, and key problem statements.

Output: Enhanced memory types, extraction prompt, and auto-refresh pipeline that captures and persists richer founder context after every chat exchange.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/REQUIREMENTS.md
@lib/fred/founder-memory-types.ts
@lib/fred/active-memory.ts
@lib/ai/memory-extraction-prompt.ts
@lib/fred/enrichment/extractor.ts
@lib/fred/context-builder.ts
@app/api/fred/chat/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend structured memory schema and extraction prompt</name>
  <files>
    lib/fred/founder-memory-types.ts
    lib/ai/memory-extraction-prompt.ts
    lib/fred/active-memory.ts
  </files>
  <action>
1. In `lib/fred/founder-memory-types.ts`:
   - Expand `CORE_MEMORY_FIELDS` to include these additional fields:
     ```
     "traction", "revenue_status", "funding_status", "team_size",
     "product_status", "ninety_day_goal", "key_decisions"
     ```
   - Update `CoreMemoryFieldKey` type (it auto-derives from CORE_MEMORY_FIELDS)
   - Add corresponding fields to the `FounderMemory` interface (each as `MemoryField`)
   - Update `emptyFounderMemory()` to include the new fields with `emptyField()` defaults

2. In `lib/ai/memory-extraction-prompt.ts`:
   - Update the MEMORY_EXTRACTION_PROMPT to include the new extractable fields in its field list:
     ```
     - traction: User count, signups, waitlist size, partnerships, pilot customers
     - revenue_status: Current revenue, MRR, ARR, or "pre-revenue"
     - funding_status: Bootstrapped, raised $X, seeking funding, etc.
     - team_size: Number of team members or "solo founder"
     - product_status: MVP, beta, launched, pre-product, etc.
     - ninety_day_goal: What they want to achieve in the next 90 days
     - key_decisions: Important decisions they've made or need to make (comma-separated)
     ```
   - Add a new extraction rule: "When the founder describes a decision they've made or a significant pivot, extract it as key_decisions"
   - Add a REPHRASE rule: "For biggest_challenge, traction, and key_decisions, rephrase the founder's words into a concise 1-sentence summary rather than quoting verbatim. Example: User says 'I keep trying to get people to sign up but nobody wants to pay' -> biggest_challenge: 'Struggling with conversion from free users to paid subscribers'"

3. In `lib/fred/active-memory.ts`:
   - Update `buildActiveFounderMemory` to populate the new fields from profile data:
     - `traction`: from enrichment_data.revenueHint or semantic "metrics/traction"
     - `revenue_status`: from profile.revenue_range or enrichment_data.revenueHint
     - `funding_status`: from profile.funding_history or enrichment_data.fundingHint
     - `team_size`: from profile.team_size (convert to string)
     - `product_status`: from semantic "product_details/product_status"
     - `ninety_day_goal`: from semantic "goals/90_day_goal"
     - `key_decisions`: from semantic "decisions/recent_decisions"
   - Update `formatMemoryBlock` to include the new fields in the prompt block output:
     ```
     **Traction:** {value}
     **Revenue:** {value}
     **Funding:** {value}
     **Team Size:** {value}
     **Product Status:** {value}
     **90-Day Goal:** {value}
     **Key Decisions:** {value}
     ```
   - Update `PROFILE_COLUMN_MAP` to include new mappings where profile columns exist:
     - `revenue_status` -> not a profile column (semantic only)
     - `team_size` -> "team_size"
     - `funding_status` -> "funding_history"
   - Update `SEMANTIC_MAP` to include new mappings:
     - `traction` -> { category: "metrics", key: "traction" }
     - `revenue_status` -> { category: "metrics", key: "revenue" }
     - `funding_status` -> { category: "startup_facts", key: "funding_status" }
     - `product_status` -> { category: "product_details", key: "product_status" }
     - `ninety_day_goal` -> { category: "goals", key: "90_day_goal" }
     - `key_decisions` -> { category: "decisions", key: "recent_decisions" }
   - Update `formatFieldLabel` to include labels for new fields

  IMPORTANT: Do NOT rename or remove any existing fields. Only ADD new ones. The existing 7 core fields (founder_name, company_name, stage, market, co_founder, biggest_challenge, oases_stage) must remain unchanged.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with zero errors
    - `npm run test -- --run lib/db/__tests__/fred-memory.test.ts` still passes (existing tests)
    - Grep `CORE_MEMORY_FIELDS` in founder-memory-types.ts shows all 14 fields
    - Grep `traction` in memory-extraction-prompt.ts confirms new field in extraction prompt
    - Grep `REPHRASE` or `rephrase` in memory-extraction-prompt.ts confirms rephrase rule
  </verify>
  <done>
    - FounderMemory interface has 14 core fields (7 original + 7 new)
    - MEMORY_EXTRACTION_PROMPT instructs LLM to extract all 14 fields with rephrase rules
    - buildActiveFounderMemory populates new fields from profile and semantic memory
    - formatMemoryBlock outputs new fields in the ACTIVE FOUNDER CONTEXT prompt section
    - persistMemoryUpdates stores new fields in both profile columns and semantic memory
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire auto-refresh pipeline into chat route post-response</name>
  <files>
    app/api/fred/chat/route.ts
    lib/fred/active-memory.ts
  </files>
  <action>
1. In `app/api/fred/chat/route.ts`, verify the existing post-response memory extraction flow:
   - The route already calls `extractMemoryUpdates(userMessage, assistantResponse)` after the streaming response completes
   - It already calls `persistMemoryUpdates(userId, updates, hasPersistentMemory)` to save extracted facts
   - **Verify** these calls are present and working. If they exist, no changes needed here.
   - If the extraction is currently only running for Pro+ tiers, update it to run for ALL tiers. The `persistMemoryUpdates` function already handles tier gating internally (profile columns update for all tiers, semantic memory only for Pro+).

2. Ensure the auto-refresh is truly non-blocking:
   - The `extractMemoryUpdates` + `persistMemoryUpdates` calls must be fire-and-forget (not awaited in the main response path)
   - They should run AFTER the SSE stream is closed, not blocking the response
   - Verify this pattern exists. If extraction is currently awaited, wrap it in a fire-and-forget:
     ```typescript
     // Fire-and-forget: extract and persist memory updates
     void (async () => {
       try {
         const updates = await extractMemoryUpdates(userMessage, fullResponse)
         if (updates.length > 0) {
           await persistMemoryUpdates(userId, updates, hasPersistentMemory)
         }
       } catch (err) {
         console.warn("[FRED Chat] Memory extraction failed (non-blocking):", err)
       }
     })()
     ```

3. Verify that `extractProfileEnrichment` (the heuristic extractor) still runs alongside the LLM-based extraction. Both should fire:
   - `extractProfileEnrichment` for fast regex-based enrichment (existing)
   - `extractMemoryUpdates` for LLM-based structured extraction (existing)
   - Both persist independently. No changes needed if both are already wired.

  IMPORTANT: Do NOT restructure the chat route. Only verify and fix the auto-refresh wiring. The route is complex and touching it minimally is critical.
  </action>
  <verify>
    - `npx tsc --noEmit` passes
    - `npm run build` succeeds (the route compiles correctly)
    - Grep `extractMemoryUpdates` in route.ts shows it's called post-response
    - Grep `persistMemoryUpdates` in route.ts shows it's called with the extracted updates
    - Grep `extractProfileEnrichment` in route.ts shows heuristic enrichment still runs
  </verify>
  <done>
    - Chat route fires LLM-based memory extraction after every response (fire-and-forget)
    - Memory updates are persisted to both profile columns (all tiers) and semantic memory (Pro+)
    - Heuristic enrichment via extractProfileEnrichment continues to run alongside
    - No regressions in the chat streaming pipeline
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run test -- --run` passes all existing tests
3. `npm run build` succeeds
4. FounderMemory now has 14 core fields covering all v8.0 memory requirements
5. The extraction prompt instructs the LLM to rephrase founder inputs (MEMORY-02)
6. Chat route auto-refreshes memory after every exchange (MEMORY-01, MEMORY-02)
</verification>

<success_criteria>
- MEMORY-01 satisfied: Structured memory captures problem statements, traction, key decisions, revenue, funding, team, product status, and 90-day goals from chat
- MEMORY-02 partially satisfied: Extraction prompt includes rephrase rules for concise storage; auto-refresh runs after every conversation
- All 14 core memory fields are populated from profile, semantic memory, and enrichment data with confidence scoring
- Memory extraction pipeline runs fire-and-forget after every chat response for all tiers
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/79/79-01-SUMMARY.md`
</output>
