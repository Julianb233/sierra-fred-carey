---
phase: 81-reality-lens-first
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/fred/reality-lens-quick.ts
  - app/dashboard/reality-lens/quick/page.tsx
  - lib/oases/stage-config.ts
autonomous: true

must_haves:
  truths:
    - "Reality Lens results determine initial Oases stage (low score -> Clarity, high score -> Validation or beyond)"
    - "Results surface gaps that motivate engagement with 'spook them' messaging"
    - "User sees their starting Oases stage with clear description of what it means"
    - "Gaps are framed as 'what you need to figure out before investors will listen'"
    - "IdeaPros-style gap awareness drives urgency to use the platform"
  artifacts:
    - path: "lib/fred/reality-lens-quick.ts"
      provides: "mapScoreToStage logic mapping score + signals to Oases stage"
      exports: ["mapScoreToStage", "quickAssessIdea"]
    - path: "app/dashboard/reality-lens/quick/page.tsx"
      provides: "Results screen with stage badge, gaps, strengths, next action"
      contains: "What you need to figure out before investors will listen"
    - path: "lib/oases/stage-config.ts"
      provides: "Stage definitions with steps including reality_lens step"
      contains: "reality_lens"
  key_links:
    - from: "lib/fred/reality-lens-quick.ts"
      to: "lib/oases/stage-config.ts"
      via: "OasesStage type used in mapScoreToStage return"
      pattern: "OasesStage"
    - from: "app/api/fred/reality-lens/quick/route.ts"
      to: "lib/db/reality-lens-state.ts"
      via: "markRealityLensComplete persists stage from assessment"
      pattern: "markRealityLensComplete.*result\\.stage"
    - from: "app/dashboard/reality-lens/quick/page.tsx"
      to: "Results display"
      via: "Gaps card with orange warning styling"
      pattern: "What you need to figure out"
---

<objective>
Verify and enhance Oases stage placement from Reality Lens scores and gap awareness messaging that motivates platform engagement.

Purpose: The quick reality check must not just assess -- it must place the founder in the right Oases stage AND surface gaps that create urgency. This is the ONBOARD-05 "spook them" requirement: make founders realize what they need to figure out before investors will listen, driving them deeper into the platform.

Output: Working stage placement from scores with compelling gap messaging in the results UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/phases/78/78-01-SUMMARY.md

Existing files to verify/enhance:
@lib/fred/reality-lens-quick.ts (mapScoreToStage + AI assessment)
@lib/fred/reality-lens-quick-shared.ts (shared types)
@app/dashboard/reality-lens/quick/page.tsx (results UI with gaps/strengths)
@lib/oases/stage-config.ts (stage definitions)
@lib/oases/ideapros-journey-steps.ts (IdeaPros ~120 step template)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and enhance stage placement logic</name>
  <files>lib/fred/reality-lens-quick.ts</files>
  <action>
    Verify the `mapScoreToStage` function correctly maps scores and signals to Oases stages per the roadmap requirements:

    1. **Read existing logic** in lib/fred/reality-lens-quick.ts:
       - No validation + idea-only -> clarity (regardless of score) -- CORRECT
       - Score < 30 -> clarity -- CORRECT
       - Score >= 80 + paying customers -> launch -- CORRECT
       - Score 60-79 + mvp/launched -> build -- CORRECT
       - Score 30-59 -> validation -- CORRECT
       - Default -> clarity -- CORRECT
       - Never returns "grow" (earned by completing all stages) -- CORRECT

    2. **Verify AI prompt** in `quickAssessIdea` includes scoring guide that produces realistic score distributions:
       - 0-29: Very early stage
       - 30-49: Some foundation
       - 50-69: Reasonable foundation
       - 70-84: Strong foundation
       - 85-100: Exceptional (rare)

    3. **Verify heuristic fallback** `getHeuristicAssessment` produces sane scores when AI fails.

    4. **Enhance the AI prompt** (if not already) to specifically instruct the LLM to identify IdeaPros-style gaps. The gaps should be specific enough to "spook" founders -- not generic advice but pointed observations about what's missing. Update the user prompt to include:
       - "Be brutally honest about gaps. Frame each gap as something an investor would immediately question."
       - "Gaps should be specific to THIS founder's situation, not generic startup advice."

    If the prompt already has sufficient gap specificity, leave it unchanged.
  </action>
  <verify>
    - Read mapScoreToStage and confirm all stage mappings match the documented rules
    - Read quickAssessIdea prompt and confirm it instructs LLM to produce specific, pointed gaps
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    Stage placement logic correctly maps all score/signal combinations to appropriate Oases stages. AI prompt produces specific, investor-perspective gap analysis rather than generic advice.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify gap awareness messaging in results UI</name>
  <files>app/dashboard/reality-lens/quick/page.tsx</files>
  <action>
    Verify the results screen in the Quick Reality Lens page delivers ONBOARD-05 "gap awareness" messaging:

    1. **Gaps card**: Confirm the results show a prominently styled card with heading "What you need to figure out before investors will listen" (already in code at line ~282). Verify:
       - Orange/red gradient background (spook them visually)
       - ExclamationTriangleIcon for urgency
       - Bullet list of specific gaps from AI assessment

    2. **Stage badge**: Confirm results show the user's assigned stage with a clear label like "Your starting point: Clarity" (already at line ~268). Verify stage descriptions explain what the stage means.

    3. **Next action**: Confirm a "#1 Next Step" card gives a single actionable recommendation (already at line ~323).

    4. **CTA buttons**: Confirm "Continue to Your Journey" goes to /dashboard (line ~337) and "Get Detailed Analysis" goes to /dashboard/reality-lens (line ~345).

    5. **Enhance messaging if needed**: If the gap awareness messaging is too soft, strengthen it. The ONBOARD-05 requirement is to "spook them" -- founders should feel urgency about their gaps, not comfort. Check that:
       - The heading uses strong language ("before investors will listen" not "areas for improvement")
       - The visual treatment is orange/warning, not neutral
       - The strengths card does NOT overshadow the gaps card (gaps should come first)

    If all the above is already correctly implemented, this task is read-and-confirm with minimal changes.
  </action>
  <verify>
    - Read the results section of quick/page.tsx
    - Confirm gaps card appears BEFORE strengths card in the DOM
    - Confirm "What you need to figure out before investors will listen" heading text
    - Confirm orange/warning gradient styling on gaps card
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    Results page delivers compelling gap awareness messaging that motivates founders to engage with the platform. Gaps are prominent, specific, and framed from an investor perspective. Stage placement is clearly displayed.
  </done>
</task>

</tasks>

<verification>
1. mapScoreToStage returns correct Oases stage for all score/signal combinations
2. AI prompt produces investor-perspective gap analysis (not generic advice)
3. Results UI shows gaps before strengths with warning styling
4. "What you need to figure out before investors will listen" heading present
5. Stage badge clearly shows starting Oases stage with description
6. CTA buttons navigate correctly (dashboard and detailed analysis)
7. TypeScript compiles: `npx tsc --noEmit`
</verification>

<success_criteria>
- Reality Lens results correctly set initial Oases stage based on score + validation signals
- Gap messaging creates urgency ("spook them") per ONBOARD-05
- Founders see exactly where they stand and what's missing
- Stage placement persisted to profiles.oases_stage
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/81/81-02-SUMMARY.md`
</output>
