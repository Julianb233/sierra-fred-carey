---
phase: 81-reality-lens-first
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/reality-lens/quick/page.tsx
  - app/api/fred/reality-lens/quick/route.ts
  - lib/fred/reality-lens-quick.ts
  - lib/db/reality-lens-state.ts
  - middleware.ts
autonomous: true

must_haves:
  truths:
    - "After completing /welcome intake, user is auto-redirected to quick reality check"
    - "Quick reality check presents 5-6 focused questions in a step-by-step flow"
    - "Completing quick check sets reality_lens_complete = true in profiles table"
    - "User who already completed reality check is redirected to dashboard (not asked again)"
    - "Middleware redirects users who have completed onboarding but not reality lens to reality lens page"
  artifacts:
    - path: "app/dashboard/reality-lens/quick/page.tsx"
      provides: "Quick reality check 6-question wizard UI"
      contains: "QuickRealityLensInner"
    - path: "app/api/fred/reality-lens/quick/route.ts"
      provides: "POST endpoint for assessment, GET for status check"
      exports: ["POST", "GET"]
    - path: "lib/db/reality-lens-state.ts"
      provides: "markRealityLensComplete and getRealityLensStatus helpers"
      exports: ["markRealityLensComplete", "getRealityLensStatus"]
    - path: "lib/fred/reality-lens-quick.ts"
      provides: "quickAssessIdea AI assessment and mapScoreToStage logic"
      exports: ["quickAssessIdea", "mapScoreToStage"]
  key_links:
    - from: "app/welcome/page.tsx"
      to: "/dashboard/reality-lens?first=true"
      via: "router.push after intake completion"
      pattern: "reality-lens.*first=true"
    - from: "app/dashboard/reality-lens/page.tsx"
      to: "/dashboard/reality-lens/quick?first=true"
      via: "router.replace on ?first=true param"
      pattern: "reality-lens/quick.*first=true"
    - from: "app/api/fred/reality-lens/quick/route.ts"
      to: "lib/db/reality-lens-state.ts"
      via: "markRealityLensComplete call after assessment"
      pattern: "markRealityLensComplete"
    - from: "middleware.ts"
      to: "/dashboard/reality-lens"
      via: "redirect for users with journey_welcomed=true but reality_lens_complete=false"
      pattern: "reality_lens_complete"
---

<objective>
Wire the quick reality check as the mandatory first interaction after onboarding, with proper redirect chain and persistence.

Purpose: After a new founder completes the /welcome intake (Phase 77), they must be funneled through a lightweight 6-question reality check before accessing the dashboard. This is the first substantive FRED experience and sets the tone for the guided venture journey.

Output: Working redirect chain (welcome -> reality-lens -> dashboard) with reality_lens_complete flag properly persisted and checked.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/phases/77/77-02-SUMMARY.md

Existing files to verify/enhance:
@app/welcome/page.tsx (redirects to reality-lens after intake -- Phase 77)
@app/dashboard/reality-lens/page.tsx (redirects first-time users to /quick)
@app/dashboard/reality-lens/quick/page.tsx (6-question wizard UI)
@app/api/fred/reality-lens/quick/route.ts (POST assessment + GET status)
@lib/db/reality-lens-state.ts (markRealityLensComplete + getRealityLensStatus)
@lib/fred/reality-lens-quick.ts (AI assessment + mapScoreToStage)
@lib/fred/reality-lens-quick-shared.ts (shared types + QUICK_QUESTIONS)
@middleware.ts (auth + welcome redirect logic)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and harden the onboarding-to-reality-lens redirect chain</name>
  <files>middleware.ts, app/welcome/page.tsx, app/dashboard/reality-lens/page.tsx, app/dashboard/reality-lens/quick/page.tsx</files>
  <action>
    Verify the existing redirect chain works end-to-end:

    1. **middleware.ts**: Confirm the middleware checks `journey_welcomed` and redirects to /welcome if false. ALSO verify that when `journey_welcomed=true` AND `reality_lens_complete=false`, the user is redirected to `/dashboard/reality-lens?first=true` (not just to the dashboard). If this redirect is missing, add it:
       - After the existing `journey_welcomed` check, add a secondary check:
       - If user is navigating to any `/dashboard` route (except `/dashboard/reality-lens`), and `reality_lens_complete` is false, redirect to `/dashboard/reality-lens?first=true`
       - Select `reality_lens_complete` alongside `journey_welcomed` in the existing profile query

    2. **app/welcome/page.tsx**: Confirm `handleIntakeComplete` redirects to `/dashboard/reality-lens?first=true` (already implemented by Phase 77-02).

    3. **app/dashboard/reality-lens/page.tsx**: Confirm the `?first=true` param triggers redirect to `/dashboard/reality-lens/quick?first=true` (already implemented).

    4. **app/dashboard/reality-lens/quick/page.tsx**: Confirm the useEffect checks status via GET /api/fred/reality-lens/quick and redirects to /dashboard if already complete (already implemented). Verify the "Continue to Your Journey" button navigates to /dashboard after results.

    The key gap to fill: middleware must enforce reality_lens_complete check so users who refresh or navigate directly cannot skip the reality check.
  </action>
  <verify>
    - Read middleware.ts and confirm `reality_lens_complete` is queried from profiles
    - Read middleware.ts and confirm redirect to `/dashboard/reality-lens?first=true` when reality_lens_complete is false
    - The redirect should NOT apply to: /dashboard/reality-lens routes themselves (would cause infinite loop), /api routes, static assets
    - `npm run build` passes (or at minimum `npx tsc --noEmit` passes)
  </verify>
  <done>
    Users who completed onboarding but not the reality check are auto-redirected to reality-lens from any dashboard page. Users who completed reality check access dashboard normally. No infinite redirect loops.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify reality_lens_complete flag persistence and status API</name>
  <files>app/api/fred/reality-lens/quick/route.ts, lib/db/reality-lens-state.ts</files>
  <action>
    Verify the existing persistence chain:

    1. **POST /api/fred/reality-lens/quick**: Confirm it calls `markRealityLensComplete(userId, result.stage, result.overallScore)` after AI assessment completes (already in code at line ~131).

    2. **lib/db/reality-lens-state.ts**: Confirm `markRealityLensComplete` updates profiles table with `reality_lens_complete: true`, `reality_lens_score`, and `oases_stage` (already implemented).

    3. **GET /api/fred/reality-lens/quick**: Confirm it returns `{ complete: boolean, stage, score }` for status checking (already implemented).

    4. **Migration check**: Confirm `supabase/migrations/20260308200001_reality_lens_columns.sql` adds `reality_lens_complete` and `reality_lens_score` columns.

    If all of the above is verified and working, this task is primarily a read-and-confirm. Only make changes if something is missing or broken.

    One thing to verify: the `markRealityLensComplete` function uses `createServiceClient()` (service role) not `createClient()` (user role). This is correct because RLS might block user-level updates to their own profile depending on policy. Confirm this is the case.
  </action>
  <verify>
    - Read lib/db/reality-lens-state.ts and confirm createServiceClient usage
    - Read the migration SQL and confirm both columns exist
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    The reality_lens_complete flag is properly set when a user completes the quick assessment, and the GET endpoint correctly reports completion status. Service client is used for reliable writes.
  </done>
</task>

</tasks>

<verification>
1. Redirect chain: /welcome -> /dashboard/reality-lens?first=true -> /dashboard/reality-lens/quick?first=true (verified in code)
2. Middleware enforces reality check for all /dashboard routes except reality-lens itself
3. reality_lens_complete flag set to true after POST assessment
4. GET status endpoint returns correct completion state
5. No TypeScript errors: `npx tsc --noEmit`
</verification>

<success_criteria>
- New users after onboarding are funneled through quick reality check before dashboard access
- reality_lens_complete flag persisted and queryable
- No redirect loops
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/81/81-01-SUMMARY.md`
</output>
