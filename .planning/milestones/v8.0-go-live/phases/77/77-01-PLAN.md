---
phase: 77-guided-venture-journey-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/welcome/journey-welcome.tsx
  - components/coaching/coaching-layout.tsx
  - components/event/event-landing.tsx
  - app/dashboard/coaching/page.tsx
  - app/dashboard/coaching/history/page.tsx
autonomous: true

must_haves:
  truths:
    - "New user sees mandatory welcome screen immediately after signup with desert/Oases metaphor"
    - "Welcome screen shows once per user and never reappears on subsequent logins"
    - "Welcome screen communicates 'This is a journey, not a transaction' framing"
    - "User sees concise orientation before interacting with Mentor (what Mentor + Progress sections do)"
    - "All onboarding UI surfaces say 'Mentor' instead of 'Fred AI'"
  artifacts:
    - path: "components/welcome/journey-welcome.tsx"
      provides: "Welcome screen with journey framing and orientation guide"
      contains: "journey, not a transaction"
    - path: "middleware.ts"
      provides: "Redirect enforcement for welcome flow"
      contains: "journey_welcomed"
  key_links:
    - from: "middleware.ts"
      to: "profiles.journey_welcomed"
      via: "Supabase query in Edge middleware"
      pattern: "journey_welcomed === false"
    - from: "app/welcome/page.tsx"
      to: "components/welcome/journey-welcome.tsx"
      via: "JourneyWelcome component import"
      pattern: "JourneyWelcome"
---

<objective>
Refine the welcome screen with "journey, not a transaction" framing, add handholding orientation content, and rename "Fred AI" to "Mentor" across onboarding UI surfaces.

Purpose: ONBOARD-01 (welcome screen), ONBOARD-04 (orientation), UILABEL-01 (Mentor rename) -- ensures first-time users understand Sahara is a guided journey before touching any tools.

Output: Updated welcome screen with explicit framing and orientation, all onboarding-facing "Fred AI" references changed to "Mentor".
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/REQUIREMENTS.md

Existing infrastructure already built:
- app/welcome/page.tsx -- Welcome page with auth gate, show-once logic via journey_welcomed flag, two-step flow (welcome -> intake)
- components/welcome/journey-welcome.tsx -- Welcome screen component with 5 Oases stages timeline, already says "This isn't a product demo. It's a guided path from idea to funded startup."
- middleware.ts (lines 81-111) -- Already redirects unauthenticated users who haven't completed welcome to /welcome, with exempt paths
- supabase/migrations/20260308000001_journey_welcomed_and_oases_stage.sql -- Already adds journey_welcomed boolean, oases_stage, co_founder, venture_timeline columns
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance welcome screen with "journey not transaction" framing and orientation guide</name>
  <files>components/welcome/journey-welcome.tsx</files>
  <action>
  Update the JourneyWelcome component to strengthen the messaging per ONBOARD-01 and add orientation content per ONBOARD-04:

  1. Change the subheading from "This isn't a product demo. It's a guided path from idea to funded startup." to explicitly include "This is a journey, not a transaction." framing. Suggested copy:
     - Heading: "Welcome to Your Venture Journey"
     - Subheading: "This is a journey, not a transaction. Think of it like crossing the Sahara -- five oases of clarity, validation, building, launching, and growing. Your mentor guides every step."

  2. Add an orientation section BELOW the 5 Oases timeline and ABOVE the warm paragraph. This section explains what the user will interact with (ONBOARD-04):
     - Two concise cards side-by-side (or stacked on mobile):
       - Card 1: "Mentor" -- "Your AI mentor learns about you and gives blunt, personalized guidance. No generic advice."
       - Card 2: "Progress" -- "Your roadmap through 5 stages. See where you are, what's next, and what you've accomplished."
     - Use the existing design patterns (motion.div with staggered delays, Sahara orange accents, Tailwind styling matching the existing component)

  3. Keep the existing CTA button "Begin My Journey" and the warm paragraph about Fred.

  Do NOT change the page.tsx wrapper, the intake form, or any routing logic. Only modify the JourneyWelcome component's content and layout.
  </action>
  <verify>
  - Run `npx tsc --noEmit` to confirm no TypeScript errors
  - Visually verify: the component renders the "journey, not a transaction" text, shows the Oases timeline, shows the Mentor + Progress orientation cards, and has the "Begin My Journey" button
  </verify>
  <done>Welcome screen explicitly communicates "journey, not a transaction" framing with desert metaphor, and shows a concise Mentor + Progress orientation section before the CTA button.</done>
</task>

<task type="auto">
  <name>Task 2: Rename "Fred AI" to "Mentor" across onboarding UI surfaces</name>
  <files>
    components/coaching/coaching-layout.tsx
    components/event/event-landing.tsx
    app/dashboard/coaching/page.tsx
    app/dashboard/coaching/history/page.tsx
  </files>
  <action>
  Rename "FRED AI" / "Fred AI" references to "Mentor" in these specific files (UILABEL-01 scope: onboarding UI surfaces only):

  1. `components/coaching/coaching-layout.tsx` line 131: Change "FRED AI sidebar" to "Mentor sidebar"
  2. `components/event/event-landing.tsx` line 34: Change `title: "FRED AI Coaching"` to `title: "Mentor Coaching"`
  3. `app/dashboard/coaching/history/page.tsx` line 189: Change "FRED AI" to "Mentor"
  4. `app/dashboard/coaching/page.tsx` line 38: Change "FRED AI assistance" to "Mentor assistance"

  Do NOT rename:
  - lib/ai/fred-client.ts (internal code comment, not user-facing)
  - lib/ai/index.ts (internal code comment, not user-facing)
  - lib/fred/tools/index.ts (internal code comment, not user-facing)
  - Any references to "Fred" as a persona name (e.g., "Your mentor Fred") -- only rename "Fred AI" as a product label
  </action>
  <verify>
  - Run `grep -rn "Fred AI\|FRED AI" --include="*.tsx" --include="*.ts" components/ app/` and confirm no user-facing matches remain (internal lib/ comments are acceptable)
  - Run `npx tsc --noEmit` to confirm no TypeScript errors
  </verify>
  <done>All user-facing "Fred AI" / "FRED AI" labels across onboarding and coaching UI surfaces are renamed to "Mentor".</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- Welcome screen at /welcome renders: "journey, not a transaction" framing, 5 Oases timeline, Mentor + Progress orientation cards, "Begin My Journey" CTA
- No user-facing "Fred AI" labels remain in onboarding surfaces (grep confirms)
- Existing middleware redirect logic (lines 81-111) still works -- unauthenticated users with journey_welcomed=false are redirected to /welcome
</verification>

<success_criteria>
1. JourneyWelcome component explicitly says "This is a journey, not a transaction" with desert metaphor
2. Orientation section shows Mentor and Progress explanations before user proceeds
3. All onboarding-facing UI surfaces say "Mentor" instead of "Fred AI"
4. No regressions in TypeScript compilation
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/77/77-01-SUMMARY.md`
</output>
