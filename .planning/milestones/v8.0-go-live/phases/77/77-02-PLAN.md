---
phase: 77-guided-venture-journey-onboarding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/welcome/intake-form.tsx
  - lib/welcome/intake-questions.ts
  - lib/welcome/types.ts
  - app/welcome/page.tsx
autonomous: true

must_haves:
  truths:
    - "User completes 5-question structured intake with free-text fields (idea, stage, challenge, goals, timeline)"
    - "FRED captures each answer, rephrases it, and stores as memory"
    - "After intake completion, user is automatically redirected to Reality Lens"
    - "Intake answers are stored in profiles table AND Fred's semantic memory"
    - "Intake uses text fields (not checkboxes) -- free-text that FRED captures and rephrases"
  artifacts:
    - path: "components/welcome/intake-form.tsx"
      provides: "5-question intake form with free-text Textarea fields"
      contains: "INTAKE_QUESTIONS"
    - path: "lib/welcome/intake-questions.ts"
      provides: "5 intake questions with fredRephrase templates"
      contains: "startup_idea"
    - path: "lib/welcome/types.ts"
      provides: "IntakeQuestion type with fredRephrase field"
      contains: "fredRephrase"
  key_links:
    - from: "components/welcome/intake-form.tsx"
      to: "/api/fred/memory"
      via: "fetch POST for each answer"
      pattern: "api/fred/memory"
    - from: "components/welcome/intake-form.tsx"
      to: "profiles table"
      via: "supabase.from('profiles').update"
      pattern: "journey_welcomed.*true"
    - from: "app/welcome/page.tsx"
      to: "/dashboard/reality-lens"
      via: "router.push after intake completion"
      pattern: "reality-lens"
---

<objective>
Verify and refine the 5-question structured intake flow with free-text fields, FRED rephrase/store behavior, and Reality Lens redirect.

Purpose: ONBOARD-03 (five-question flow), UILABEL-06 (text field conversion) -- ensures new accounts complete structured intake with free-text answers that FRED captures, rephrases, and stores as memory before being redirected to Reality Lens.

Output: Verified and refined intake form, intake questions, and redirect behavior.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/REQUIREMENTS.md

Existing infrastructure already built:
- components/welcome/intake-form.tsx -- IntakeForm component with 5 free-text Textarea questions, FRED rephrase display, saves to /api/fred/memory and profiles table, sets journey_welcomed=true
- lib/welcome/intake-questions.ts -- 5 questions: startup_idea, current_stage, biggest_challenge, co_founder, timeline_goal with fredRephrase templates and memoryCategory/memoryKey mappings
- lib/welcome/types.ts -- IntakeQuestion type (id, question, placeholder, fredRephrase, memoryCategory, memoryKey, profileField), IntakeAnswers type, IntakeStep type
- app/welcome/page.tsx -- Orchestrates welcome -> intake flow, redirects to /dashboard/reality-lens?first=true on completion
- app/api/fred/memory -- API endpoint for storing FRED semantic memory facts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and refine intake form behavior and question content</name>
  <files>
    components/welcome/intake-form.tsx
    lib/welcome/intake-questions.ts
  </files>
  <action>
  The intake form infrastructure is already built. Review and make these targeted refinements:

  1. In `lib/welcome/intake-questions.ts`:
     - Verify all 5 questions match the requirements: idea, stage, challenge, goals, timeline
     - The current question #4 asks about co-founder. Per ONBOARD-03, question #4 should ask about goals. Update:
       - Reorder so the questions are: (1) startup_idea, (2) current_stage, (3) biggest_challenge, (4) goals/objectives, (5) timeline
       - Change question #4 from co-founder to goals: `id: 'goals'`, question: "What are your top goals for this venture?", placeholder: "What does success look like for you in the next 6-12 months?", fredRephrase: "Your goals: {answer}. Now I know exactly what we're aiming for.", memoryCategory: 'goals', memoryKey: 'venture_goals'
       - Move co-founder to be captured separately OR keep as question #5 replacing timeline. Actually, per the requirements, the 5 questions are: idea, stage, challenge, goals, timeline. Keep timeline as #5.
       - Remove the co_founder question entirely -- it's not in the ONBOARD-03 spec. Co-founder info can be captured later in chat.

  2. In `lib/welcome/types.ts`:
     - Update IntakeAnswers to match: startup_idea, current_stage, biggest_challenge, goals, timeline_goal

  3. In `components/welcome/intake-form.tsx`:
     - Update the `processAnswers` function: remove the `co_founder` profile field update since that question is removed
     - Keep all other behavior: FRED rephrase display, memory storage, journey_welcomed flag, normalizeStage for stage field
     - The form already uses Textarea (free-text) inputs -- this satisfies UILABEL-06 (no checkboxes)

  Do NOT change:
  - The overall form UX flow (question-by-question with FRED rephrase animation)
  - The /api/fred/memory integration
  - The journey_welcomed flag logic
  - The redirect behavior in app/welcome/page.tsx
  </action>
  <verify>
  - Run `npx tsc --noEmit` to confirm no TypeScript errors
  - Verify intake-questions.ts has exactly 5 questions: startup_idea, current_stage, biggest_challenge, goals, timeline_goal
  - Verify IntakeForm.processAnswers no longer references co_founder
  </verify>
  <done>Intake form has exactly 5 free-text questions matching ONBOARD-03 spec (idea, stage, challenge, goals, timeline), FRED rephrases each answer, and all answers are stored in memory and profiles.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Reality Lens redirect and end-to-end flow integrity</name>
  <files>
    app/welcome/page.tsx
  </files>
  <action>
  Verify the end-to-end welcome flow works correctly:

  1. In `app/welcome/page.tsx`:
     - Confirm `handleIntakeComplete` redirects to `/dashboard/reality-lens?first=true` (already does -- line 53)
     - Confirm auth gate redirects unauthenticated users to /get-started (already does -- line 25)
     - Confirm show-once logic checks journey_welcomed and redirects already-welcomed users to /dashboard (already does -- lines 30-39)

  2. Add a brief loading state improvement: after intake completes and before redirect, show a brief "Preparing your Reality Lens..." message. Update the `handleIntakeComplete` to set a local `redirecting` state that shows a transition message for 1 second before router.push. This prevents a jarring jump.
     - Add `const [redirecting, setRedirecting] = useState(false)` state
     - In handleIntakeComplete: set redirecting=true, then after 800ms call router.push("/dashboard/reality-lens?first=true")
     - When redirecting=true, show a centered message: "Preparing your first reality check..." with a Loader2 spinner, using the same bg gradient as the page

  3. Verify the `app/dashboard/reality-lens/page.tsx` exists (it does at app/dashboard/reality-lens/) so the redirect target is valid.

  Do NOT change the middleware enforcement, the intake form component, or the JourneyWelcome component.
  </action>
  <verify>
  - Run `npx tsc --noEmit` to confirm no TypeScript errors
  - Verify app/welcome/page.tsx: auth gate works, show-once works, intake -> Reality Lens redirect works with transition
  - Verify /dashboard/reality-lens route exists
  </verify>
  <done>End-to-end flow verified: signup -> /welcome (auth-gated, show-once) -> welcome screen -> 5 questions -> FRED stores answers -> smooth transition -> /dashboard/reality-lens redirect.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- Intake form has exactly 5 free-text questions: idea, stage, challenge, goals, timeline
- Each answer is stored via /api/fred/memory with appropriate category/key
- Profile is updated with journey_welcomed=true after all answers
- Redirect to /dashboard/reality-lens?first=true happens after intake completion
- Show-once logic prevents welcome from reappearing
</verification>

<success_criteria>
1. New user completes 5-question intake with free-text Textarea fields (not checkboxes)
2. FRED captures, rephrases (shown inline), and stores each answer as semantic memory
3. After intake, user sees brief transition then is redirected to Reality Lens
4. Welcome screen never reappears for users who have completed it
5. No TypeScript compilation errors
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/77/77-02-SUMMARY.md`
</output>
