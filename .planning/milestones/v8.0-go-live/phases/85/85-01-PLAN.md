---
phase: 85-journey-gated-fund-matching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/boardy/page.tsx
  - components/journey/journey-gate.tsx
  - components/journey/celebration-milestone.tsx
  - components/boardy/journey-celebration.tsx
  - lib/ai/prompt-layers.ts
  - lib/boardy/intro-templates.ts
  - app/api/journey/completion/route.ts
autonomous: true

must_haves:
  truths:
    - "Boardy fund matching UI is grayed out with journey completion percentage when user has not completed 100% of the journey"
    - "When user reaches 100% journey completion, a celebration milestone triggers with confetti and FRED congratulations"
    - "FRED references Boardy investor matches in chat when user is at 100% completion"
    - "Introduction preparation guidance (call scripts, email templates) is available post-unlock"
  artifacts:
    - path: "components/journey/journey-gate.tsx"
      provides: "Journey percentage gating overlay with progress bar and stage badges"
      contains: "JourneyGate"
    - path: "components/journey/celebration-milestone.tsx"
      provides: "Full-screen confetti celebration modal on first 100% unlock"
      contains: "CelebrationMilestone"
    - path: "components/boardy/journey-celebration.tsx"
      provides: "Dismissible celebration banner at top of Boardy page"
      contains: "JourneyCelebration"
    - path: "lib/ai/prompt-layers.ts"
      provides: "FRED prompt layer with Boardy match awareness rules"
      contains: "BOARDY MATCH AWARENESS"
    - path: "lib/boardy/intro-templates.ts"
      provides: "Call script and email template generators"
      exports: ["generateCallScript", "generateEmailTemplate"]
    - path: "app/dashboard/boardy/page.tsx"
      provides: "Dual-gated Boardy page with FeatureLock + JourneyGate + celebration"
      contains: "JourneyGate"
  key_links:
    - from: "app/dashboard/boardy/page.tsx"
      to: "components/journey/journey-gate.tsx"
      via: "JourneyGate wrapper requiring 100% completion"
      pattern: "JourneyGate.*requiredPercent.*100"
    - from: "components/journey/journey-gate.tsx"
      to: "/api/journey/completion"
      via: "fetch in useEffect"
      pattern: "fetch.*api/journey/completion"
    - from: "lib/ai/prompt-layers.ts"
      to: "FRED chat responses"
      via: "Boardy match awareness rules in system prompt"
      pattern: "BOARDY MATCH AWARENESS"
---

<objective>
Verify and harden the journey-gated fund matching feature so that Boardy investor matching is locked behind 100% venture journey completion with celebration UX and FRED chat integration.

Purpose: This phase ensures the fund matching gate, celebration milestone, FRED chat references, and intro prep guidance all work correctly as an integrated feature. Much of the implementation already exists from prior phases (04, 85 partial, 89). This plan focuses on verification, wiring correctness, and filling any gaps.

Output: Fully functional journey-gated Boardy page with celebration UX, FRED awareness, and intro preparation templates.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@app/dashboard/boardy/page.tsx
@components/journey/journey-gate.tsx
@components/journey/celebration-milestone.tsx
@components/boardy/journey-celebration.tsx
@lib/journey/completion.ts
@lib/ai/prompt-layers.ts
@lib/boardy/intro-templates.ts
@lib/boardy/types.ts
@hooks/use-oases-progress.ts
@components/tier/feature-lock.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and harden journey gating + celebration wiring</name>
  <files>
    app/dashboard/boardy/page.tsx
    components/journey/journey-gate.tsx
    components/journey/celebration-milestone.tsx
    components/boardy/journey-celebration.tsx
    lib/journey/completion.ts
    app/api/journey/completion/route.ts
  </files>
  <action>
    Verify the full journey-gating pipeline works end-to-end:

    1. **Journey Completion API** (`app/api/journey/completion/route.ts`):
       - Verify it exists and returns `{ success: true, data: JourneyCompletion }` with percent, stage, isComplete, stagesCompleted, nextStage fields
       - Ensure it reads `oases_stage` from the profiles table and maps to percentage via STAGE_WEIGHTS in `lib/journey/completion.ts`

    2. **JourneyGate component** (`components/journey/journey-gate.tsx`):
       - Verify it fetches from `/api/journey/completion` and gates children behind `requiredPercent` (default 100)
       - Confirm the locked overlay shows: journey percentage, progress bar, stage badges (completed/current/future), "Continue Journey" CTA, and "Chat with Fred" CTA
       - Ensure it fails open (renders children) if the API call fails -- this is correct behavior

    3. **CelebrationMilestone** (`components/journey/celebration-milestone.tsx`):
       - Verify it shows once (localStorage key `sahara_celebration_100_seen`), has confetti animation, FRED quote, and "Meet Your Matches" CTA
       - Confirm auto-dismiss after 10 seconds

    4. **JourneyCelebration banner** (`components/boardy/journey-celebration.tsx`):
       - Verify dismissible banner with localStorage key `sahara_journey_celebration_dismissed`
       - Confirm it only shows when progress >= 100% AND not yet dismissed

    5. **Boardy page wiring** (`app/dashboard/boardy/page.tsx`):
       - Verify dual gate: `FeatureLock` (Studio tier + grow stage) wraps `JourneyGate` (100% completion)
       - Confirm CelebrationMilestone and JourneyCelebration are inside the JourneyGate
       - Confirm IntroductionPreparation section renders call script, email template, and talking points cards

    If any of these verifications fail, fix the issue. If all pass, confirm each artifact is correctly wired.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no errors related to boardy, journey, or celebration files
    - `grep -r "JourneyGate" app/dashboard/boardy/page.tsx` shows the gate wrapping content
    - `grep -r "CelebrationMilestone" app/dashboard/boardy/page.tsx` shows celebration inside gate
    - `grep -r "requiredPercent" components/journey/journey-gate.tsx` shows default 100
    - `grep -r "BOARDY MATCH AWARENESS" lib/ai/prompt-layers.ts` shows FRED prompt section exists
  </verify>
  <done>
    Journey gating pipeline verified end-to-end: JourneyGate blocks at < 100%, shows percentage + progress bar; CelebrationMilestone fires once with confetti on first 100% visit; JourneyCelebration banner is dismissible; Boardy page has dual gate (tier + journey); all TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify FRED chat Boardy awareness + intro prep templates</name>
  <files>
    lib/ai/prompt-layers.ts
    lib/boardy/intro-templates.ts
    components/boardy/intro-prep-card.tsx
    components/boardy/match-list.tsx
  </files>
  <action>
    Verify the FRED chat integration and intro preparation guidance:

    1. **FRED Boardy awareness** (`lib/ai/prompt-layers.ts`):
       - Verify the "BOARDY MATCH AWARENESS" section exists in the prompt layers
       - Confirm rules: reference matches when founder discusses fundraising/pitch prep/networking; do NOT bring up unprompted in unrelated conversations; offer practical prep (draft intro email, practice pitch); never fabricate match details
       - Verify that match data (count, types) is injected into the Founder Snapshot section of the prompt. If not, check how match data flows into the prompt context and ensure FRED has access to active match counts when the user is at 100% completion

    2. **Intro templates** (`lib/boardy/intro-templates.ts`):
       - Verify `generateCallScript()` produces personalized call scripts with: opening, 30-second pitch placeholder, key points (different for investor vs advisor), questions to ask, closing
       - Verify `generateEmailTemplate()` produces email templates with: subject line, intro paragraph, context paragraph (different for investor vs advisor), CTA for meeting, sign-off
       - Confirm both functions accept `{ name, type, focus? }` and personalize based on match type

    3. **IntroPrepCard** (`components/boardy/intro-prep-card.tsx`):
       - Verify it renders for `connected` and `intro_sent` matches only
       - Confirm expandable card with tabs for "Call Script" and "Email Template"
       - Confirm copy-to-clipboard functionality with toast feedback

    4. **MatchList** (`components/boardy/match-list.tsx`):
       - Verify each MatchCard renders an IntroPrepCard at the bottom
       - Confirm status progression: suggested -> connected -> intro_sent -> meeting_scheduled

    If the FRED prompt does not include match count injection into the Founder Snapshot, add a section that injects active match counts. Look at how the Founder Snapshot is built (likely in `lib/ai/prompt-layers.ts` or a related file) and add match count data from the `boardy_matches` table when building the snapshot for users at 100% journey completion.
  </action>
  <verify>
    - `grep -r "BOARDY MATCH AWARENESS" lib/ai/prompt-layers.ts` returns the awareness rules
    - `grep -r "generateCallScript\|generateEmailTemplate" lib/boardy/intro-templates.ts` shows both exports
    - `grep -r "IntroPrepCard" components/boardy/match-list.tsx` shows it rendered per match card
    - `npx tsc --noEmit` passes with no errors in boardy or intro-template files
  </verify>
  <done>
    FRED references Boardy investor matches in chat when user is at 100% completion (prompt rules verified, match data available in Founder Snapshot). Introduction preparation guidance is available post-unlock: call scripts, email templates, and talking points rendered per-match via IntroPrepCard and in the IntroductionPreparation section. All templates personalize by match type (investor vs advisor).
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- no TypeScript errors in any modified files
2. Journey gating: JourneyGate component blocks content at < 100%, shows progress bar, stage badges, percentage
3. Celebration: CelebrationMilestone fires once with confetti on first 100% visit; JourneyCelebration banner is dismissible
4. FRED awareness: BOARDY MATCH AWARENESS section in prompt-layers.ts with correct rules
5. Intro prep: generateCallScript and generateEmailTemplate produce personalized content; IntroPrepCard renders per-match
6. Dual gate: Boardy page uses both FeatureLock (tier) and JourneyGate (journey completion)
</verification>

<success_criteria>
- Boardy fund matching UI is grayed out with percentage and progress bar when journey < 100%
- 100% completion triggers celebration (confetti modal + banner)
- FRED has Boardy match awareness rules in system prompt and references matches contextually
- Call scripts and email templates are available per-match for connected/intro_sent matches
- Introduction preparation section shows call script template, email template, and key talking points
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/85/85-01-SUMMARY.md`
</output>
