---
phase: 84-daily-mentor-guidance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/sms/daily-guidance.ts
  - app/api/cron/daily-guidance/route.ts
  - lib/sms/fred-sms-handler.ts
  - lib/sms/templates.ts
  - vercel.json
autonomous: true

must_haves:
  truths:
    - "Proactive SMS delivers daily guidance to founder's phone each morning"
    - "SMS contains the same 3 prescriptive tasks as the dashboard widget"
    - "SMS includes a link back to the platform for full details"
    - "Founders can reply to SMS and get routed through FRED chat engine"
    - "Only Pro+ users with SMS enabled and a verified phone receive daily SMS"
  artifacts:
    - path: "lib/sms/daily-guidance.ts"
      provides: "SMS formatting and delivery for daily guidance"
      exports: ["sendDailyGuidanceSMS", "getEligibleUsersForSMS"]
    - path: "app/api/cron/daily-guidance/route.ts"
      provides: "Cron endpoint that dispatches daily guidance SMS in batches"
      exports: ["GET"]
    - path: "lib/sms/fred-sms-handler.ts"
      provides: "Inbound SMS processing through FRED chat engine (platform return flow)"
      exports: ["processFredSMS", "formatForSMS"]
  key_links:
    - from: "app/api/cron/daily-guidance/route.ts"
      to: "lib/guidance/daily-agenda.ts"
      via: "generateDailyAgenda per eligible user"
      pattern: "generateDailyAgenda"
    - from: "app/api/cron/daily-guidance/route.ts"
      to: "lib/sms/daily-guidance.ts"
      via: "sendDailyGuidanceSMS after generating agenda"
      pattern: "sendDailyGuidanceSMS"
    - from: "lib/sms/daily-guidance.ts"
      to: "lib/sms/client.ts"
      via: "sendSMS for Twilio delivery"
      pattern: "sendSMS"
    - from: "lib/sms/fred-sms-handler.ts"
      to: "lib/fred/service.ts"
      via: "createFredService for inbound SMS replies"
      pattern: "createFredService"
---

<objective>
Wire up Twilio SMS delivery for daily guidance and verify the platform return flow (inbound SMS replies routed through FRED).

Purpose: Founders receive their daily 3 tasks via SMS each morning. When they reply, the conversation flows through FRED's chat engine, creating a cost-effective SMS-to-platform loop that drives re-engagement.

Output: Working cron job that sends daily guidance SMS to Pro+ users, with inbound reply handling through FRED.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/phases/84/84-01-PLAN.md

Existing files already built (verify and enhance):
@lib/sms/daily-guidance.ts
@app/api/cron/daily-guidance/route.ts
@lib/sms/fred-sms-handler.ts
@lib/sms/client.ts
@lib/sms/templates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and harden Twilio SMS delivery pipeline</name>
  <files>
    lib/sms/daily-guidance.ts
    app/api/cron/daily-guidance/route.ts
    vercel.json
  </files>
  <action>
    1. Verify `lib/sms/daily-guidance.ts`:
       - `formatAgendaForSMS()` formats agenda as personalized SMS with task list and platform link
       - `sendDailyGuidanceSMS()` handles missing Twilio credentials gracefully (warns, skips)
       - `getEligibleUsersForSMS()` filters to Pro+ tier with SMS enabled and verified phone
       - SMS stays under 480 chars (3 segments max)
       - Includes deep link back to dashboard: `${APP_URL}/dashboard`

    2. Verify `app/api/cron/daily-guidance/route.ts`:
       - Authenticates via CRON_SECRET (timing-safe comparison)
       - Gets eligible users, generates agenda per user, sends SMS
       - Processes in batches of 10 with delay between batches
       - Logs sent/failed counts
       - Returns `{ success, processed, sent, failed }`

    3. Check `vercel.json` for cron configuration. If daily-guidance cron is NOT configured, add it:
       ```json
       {
         "crons": [
           {
             "path": "/api/cron/daily-guidance",
             "schedule": "0 15 * * *"
           }
         ]
       }
       ```
       The schedule `0 15 * * *` = 8am PT (15:00 UTC) daily.
       If vercel.json already has crons, merge the new entry. Do NOT overwrite existing cron entries.

    4. Verify `lib/sms/client.ts` exports `sendSMS()` with Twilio Messaging Service SID support and status callback URL.

    5. If any pieces are missing or incomplete, implement them. If everything is correct, leave as-is.
  </action>
  <verify>
    - `grep -n "formatAgendaForSMS" lib/sms/daily-guidance.ts` shows formatting function
    - `grep -n "CRON_SECRET" app/api/cron/daily-guidance/route.ts` shows auth check
    - `grep -n "BATCH_SIZE" app/api/cron/daily-guidance/route.ts` shows batch processing
    - `grep -n "daily-guidance" vercel.json` shows cron is configured (or cron config exists elsewhere)
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    Cron job at 8am PT sends personalized daily guidance SMS to all eligible Pro+ users. SMS contains 3 task titles and a platform link. Twilio credentials are handled gracefully when missing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify platform return flow (inbound SMS via FRED)</name>
  <files>
    lib/sms/fred-sms-handler.ts
    lib/sms/templates.ts
  </files>
  <action>
    1. Verify `lib/sms/fred-sms-handler.ts`:
       - `processFredSMS()` routes inbound SMS through FRED chat engine via `createFredService`
       - Builds founder context with `buildFounderContext(userId, true)`
       - Adds SMS-specific channel rules (under 400 chars, no markdown, numbered actions)
       - Stores both inbound and outbound messages in episodic memory with `channel: "sms"` tag
       - Handles errors gracefully (sends friendly error SMS, doesn't crash)
       - `formatForSMS()` strips markdown and truncates to SMS length

    2. Verify the SMS webhook route exists at `app/api/sms/webhook/route.ts` or similar:
       - Receives Twilio webhook POST with `From`, `Body`, `To` params
       - Looks up user by phone number
       - Calls `processFredSMS(userId, phone, body)`
       - Returns TwiML response
       If the webhook route doesn't exist, create it at `app/api/sms/webhook/route.ts`.

    3. Verify the daily guidance SMS includes a clear CTA driving users back to the platform:
       - `lib/sms/daily-guidance.ts` should include "Open Sahara to see details: {url}/dashboard"
       - This creates the SMS -> platform return loop (GUIDE-04)

    4. Verify `lib/sms/templates.ts` exists with template functions (this supports the broader SMS system).

    5. If any pieces are missing, implement them. If already correct, leave as-is.
  </action>
  <verify>
    - `grep -n "processFredSMS" lib/sms/fred-sms-handler.ts` shows FRED SMS handler
    - `grep -n "channel.*sms" lib/sms/fred-sms-handler.ts` shows channel tagging
    - `grep -rn "processFredSMS" app/api/sms/` shows webhook calls handler
    - `grep -n "dashboard" lib/sms/daily-guidance.ts` shows platform return link
    - `npx tsc --noEmit` passes
  </verify>
  <done>
    Founders can reply to daily guidance SMS and get routed through FRED's chat engine. Replies are stored in episodic memory with SMS channel tag. Daily guidance SMS includes platform deep link for return flow. Complete SMS -> FRED -> platform loop works.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `grep -rn "sendDailyGuidanceSMS" lib/ app/` shows SMS delivery is wired to cron
3. `grep -rn "processFredSMS" lib/ app/` shows inbound SMS handling exists
4. `grep -rn "CRON_SECRET" app/api/cron/daily-guidance/` shows cron auth
5. `grep -n "dashboard" lib/sms/daily-guidance.ts` shows platform return link in SMS
6. Cron schedule configured for daily morning delivery
</verification>

<success_criteria>
- Cron job dispatches daily guidance SMS to Pro+ users at 8am PT
- SMS contains personalized 3-task summary with founder's name
- SMS includes platform deep link (joinsahara.com/dashboard)
- Inbound SMS replies are processed through FRED chat engine
- SMS conversations are stored in episodic memory with channel tag
- Missing Twilio credentials are handled gracefully (warn + skip, no crash)
- Batch processing prevents Twilio rate limiting
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/84/84-02-SUMMARY.md`
</output>
