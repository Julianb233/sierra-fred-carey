---
phase: 84-daily-mentor-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/guidance/daily-agenda.ts
  - lib/guidance/types.ts
  - components/dashboard/daily-agenda-widget.tsx
  - app/api/dashboard/daily-agenda/route.ts
  - app/dashboard/page.tsx
  - supabase/migrations/20260309_daily_agendas.sql
autonomous: true

must_haves:
  truths:
    - "Dashboard displays 'Today, accomplish these 3 things' widget with AI-generated tasks"
    - "Tasks reflect founder's actual Oases stage, challenges, and recent chat history"
    - "Tasks refresh daily (cached per user per day, regenerated next day)"
    - "Task completion can be logged and feeds back into journey progress via journey events"
  artifacts:
    - path: "lib/guidance/daily-agenda.ts"
      provides: "AI-powered daily agenda generation with Oases-stage-aware prompts"
      exports: ["generateDailyAgenda", "logTaskCompletion"]
    - path: "lib/guidance/types.ts"
      provides: "DailyAgenda and DailyTask type definitions"
      exports: ["DailyAgenda", "DailyTask"]
    - path: "components/dashboard/daily-agenda-widget.tsx"
      provides: "Dashboard widget rendering 3 daily tasks with completion checkboxes"
    - path: "app/api/dashboard/daily-agenda/route.ts"
      provides: "GET (fetch agenda) and POST (log completion) API endpoints"
      exports: ["GET", "POST"]
    - path: "supabase/migrations/20260309_daily_agendas.sql"
      provides: "daily_agendas table with user_id, date, tasks JSONB, completed_tasks"
  key_links:
    - from: "components/dashboard/daily-agenda-widget.tsx"
      to: "/api/dashboard/daily-agenda"
      via: "fetch in useEffect + POST on checkbox click"
      pattern: "fetch.*api/dashboard/daily-agenda"
    - from: "lib/guidance/daily-agenda.ts"
      to: "lib/db/fred-memory"
      via: "retrieveRecentEpisodes for chat context"
      pattern: "retrieveRecentEpisodes"
    - from: "lib/guidance/daily-agenda.ts"
      to: "lib/ai/providers"
      via: "generateObject with tier-routed model"
      pattern: "generateObject"
    - from: "lib/guidance/daily-agenda.ts"
      to: "lib/db/journey-events"
      via: "logJourneyEventAsync on task completion"
      pattern: "logJourneyEventAsync"
---

<objective>
Wire up the daily mentor guidance system: AI-generated daily agenda on the dashboard with task completion tracking.

Purpose: Founders open their dashboard and see exactly what to do today -- FRED tells them, they don't have to ask. Tasks are personalized to their Oases stage, recent conversations, and outstanding commitments.

Output: Working daily agenda widget on dashboard, backed by AI generation engine, with task completion logging that feeds journey progress.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/milestones/v8.0-go-live/phases/78/78-01-SUMMARY.md
@.planning/milestones/v8.0-go-live/phases/79/79-01-SUMMARY.md

Existing files already built (verify and enhance):
@lib/guidance/types.ts
@lib/guidance/daily-agenda.ts
@components/dashboard/daily-agenda-widget.tsx
@app/api/dashboard/daily-agenda/route.ts
@app/dashboard/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify and harden daily agenda engine + database table</name>
  <files>
    lib/guidance/daily-agenda.ts
    lib/guidance/types.ts
    supabase/migrations/20260309_daily_agendas.sql
  </files>
  <action>
    1. Check if the `daily_agendas` table exists in Supabase. If not, create a migration:
       ```sql
       CREATE TABLE IF NOT EXISTS daily_agendas (
         id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
         user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE NOT NULL,
         date date NOT NULL,
         tasks jsonb NOT NULL DEFAULT '[]',
         completed_tasks text[] NOT NULL DEFAULT '{}',
         oases_stage text,
         created_at timestamptz DEFAULT now(),
         updated_at timestamptz DEFAULT now(),
         UNIQUE(user_id, date)
       );
       CREATE INDEX idx_daily_agendas_user_date ON daily_agendas(user_id, date);
       ALTER TABLE daily_agendas ENABLE ROW LEVEL SECURITY;
       CREATE POLICY "Users can read own agendas" ON daily_agendas FOR SELECT USING (auth.uid() = user_id);
       ```

    2. Verify `lib/guidance/daily-agenda.ts` correctly:
       - Loads founder profile including `oases_stage` from Phase 78
       - Loads recent chat episodes from Phase 79 memory layer
       - Loads outstanding next-steps for accountability
       - Uses `generateObject` with tier-routed model
       - Caches per user per day (upsert on user_id + date)
       - Falls back to generic tasks on AI failure
       - Logs `daily_task_completed` journey events on completion

    3. Verify `lib/guidance/types.ts` has complete DailyTask and DailyAgenda types with all fields the widget needs (id, title, description, priority, estimatedMinutes, category, completed).

    4. Ensure the `loadFounderProfile` function includes `oases_stage` in its select query and the prompt references it correctly for stage-aware task generation.

    5. If any of the above are missing or incomplete, implement them. If they're already correct, leave them as-is.
  </action>
  <verify>
    - `npx tsc --noEmit` passes with no errors in guidance files
    - `grep -n "oases_stage" lib/guidance/daily-agenda.ts` shows stage-aware logic
    - `grep -n "logJourneyEventAsync" lib/guidance/daily-agenda.ts` shows journey event logging
    - `grep -n "retrieveRecentEpisodes" lib/guidance/daily-agenda.ts` shows memory layer integration
    - Migration file exists if table didn't exist
  </verify>
  <done>
    Daily agenda engine generates personalized 3-task agendas using founder's Oases stage, recent chat history, and outstanding commitments. Task completions log journey events. Database table exists with RLS.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify dashboard widget integration and API endpoints</name>
  <files>
    components/dashboard/daily-agenda-widget.tsx
    app/api/dashboard/daily-agenda/route.ts
    app/dashboard/page.tsx
  </files>
  <action>
    1. Verify `components/dashboard/daily-agenda-widget.tsx`:
       - Fetches from `/api/dashboard/daily-agenda` on mount
       - Renders 3 task cards with priority badges, category icons, estimated time
       - Checkbox click POSTs taskId to same endpoint
       - Shows loading skeleton, empty state, and "all complete" celebration
       - Uses Sahara brand colors (#ff6a1a)

    2. Verify `app/api/dashboard/daily-agenda/route.ts`:
       - GET: Requires auth, rate-limited, calls `generateDailyAgenda(userId)`
       - POST: Requires auth, validates `taskId`, calls `logTaskCompletion(userId, taskId)`
       - Returns `{ success: true, data: agenda }` format

    3. Verify `app/dashboard/page.tsx` renders `<DailyAgendaWidget />` prominently (near top, after FredHero). It should be wrapped in `<FadeIn>` for animation.

    4. If the widget is NOT imported or rendered on the dashboard, add it. If it IS already there (check import + JSX), leave as-is.

    5. Run `npm run build` to confirm no compilation errors.
  </action>
  <verify>
    - `grep -n "DailyAgendaWidget" app/dashboard/page.tsx` shows import and usage
    - `grep -n "daily-agenda" app/api/dashboard/daily-agenda/route.ts` confirms route exists
    - `npm run build` succeeds (or at minimum `npx tsc --noEmit` passes)
  </verify>
  <done>
    Dashboard displays daily agenda widget prominently. GET endpoint returns AI-generated agenda. POST endpoint logs task completion. Widget shows loading state, task cards, and completion celebration.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes
2. `grep -rn "DailyAgendaWidget" components/ app/` shows widget is imported and rendered
3. `grep -rn "generateDailyAgenda" lib/ app/` shows engine is called from API route
4. `grep -rn "logTaskCompletion" lib/ app/` shows completion tracking is wired
5. `grep -rn "logJourneyEventAsync" lib/guidance/` shows journey event integration
6. Daily agendas table has RLS policy
</verification>

<success_criteria>
- Dashboard shows "Today's Focus" widget with 3 AI-generated tasks based on founder's Oases stage and chat history
- Tasks display priority (must-do/should-do/stretch), category, and estimated time
- Clicking a task checkbox marks it complete via POST API and updates UI
- Completing all 3 tasks shows celebration toast
- Agenda is cached per user per day (no redundant AI calls)
- Task completion fires `daily_task_completed` journey event
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/84/84-01-SUMMARY.md`
</output>
