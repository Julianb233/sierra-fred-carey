---
phase: 87-pitch-deck-scoring
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/dashboard/documents/page.tsx
  - app/api/dashboard/deck-review/route.ts
autonomous: true

must_haves:
  truths:
    - "Feature is gated to Pro+ tier via FeatureLock"
    - "Documents page passes requiredStage='build' to FeatureLock for Oases stage-gating"
    - "API route enforces stage-gate check (Build stage or later required)"
    - "Upload-parse-score completes within 60 seconds for a 15-slide deck"
  artifacts:
    - path: "app/dashboard/documents/page.tsx"
      provides: "Stage-gated documents page with FeatureLock requiredStage"
      contains: "requiredStage"
    - path: "app/api/dashboard/deck-review/route.ts"
      provides: "API with both tier and stage-gate enforcement"
      contains: "oases_stage"
  key_links:
    - from: "app/dashboard/documents/page.tsx"
      to: "components/tier/feature-lock.tsx"
      via: "FeatureLock requiredStage prop"
      pattern: "requiredStage.*build"
    - from: "app/api/dashboard/deck-review/route.ts"
      to: "lib/oases/stage-config.ts"
      via: "stage validation"
      pattern: "oases_stage|stage.*build"
---

<objective>
Add Oases stage-gating to pitch deck scoring and verify performance.

Purpose: Phase 87 depends on Phase 80 (stage-gating). Pitch deck upload must be gated to the "Build" Oases stage or later, in addition to the existing Pro+ tier gate. This plan wires stage-gating into both UI and API, and verifies the 60-second performance requirement.

Output: Stage-gated pitch deck feature that enforces both tier and journey progression.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@app/dashboard/documents/page.tsx
@app/api/dashboard/deck-review/route.ts
@components/tier/feature-lock.tsx
@lib/oases/stage-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add stage-gating to Documents page and API</name>
  <files>app/dashboard/documents/page.tsx, app/api/dashboard/deck-review/route.ts</files>
  <action>
  **UI Stage-Gating (documents/page.tsx):**

  1. The page already uses `FeatureLock` with `requiredTier={UserTier.PRO}`. Add stage-gating:
     - Import `OasesStage` from `@/types/oases`
     - The page needs the user's current Oases stage. Check if `useUserTier()` or another existing hook provides `oasesStage`. If not, add a `useState` + `useEffect` that fetches the user's stage from an existing API (check `/api/dashboard/stats` or profile endpoint for `oases_stage` field).
     - Add `requiredStage={"build" as OasesStage}` and `currentStage={userStage}` props to the `FeatureLock` component.
     - This means users in Clarity or Validation stages see the stage-lock message instead of the documents page.

  **API Stage-Gating (deck-review/route.ts):**

  2. After the existing tier check, add a stage-gate check:
     - Query the user's `oases_stage` from `profiles` table: `SELECT oases_stage FROM profiles WHERE id = ${userId}`
     - Import `STAGE_ORDER` from `@/lib/oases/stage-config`
     - Check if user's stage index is >= "build" stage index: `STAGE_ORDER.indexOf(userStage) >= STAGE_ORDER.indexOf("build")`
     - If not, return 403: `{ success: false, error: "Pitch Deck Scoring is available once you reach the Build stage of your venture journey" }`
     - This prevents API bypass even if someone calls the endpoint directly.

  **Important:** Use the existing `sql` import already in the route file for the query. The `profiles` table has an `oases_stage` text column (values: 'clarity', 'validation', 'build', 'launch', 'grow').
  </action>
  <verify>
  Run `npx tsc --noEmit` to confirm types compile. Grep for `requiredStage` in `app/dashboard/documents/page.tsx` and `oases_stage` in `app/api/dashboard/deck-review/route.ts`.
  </verify>
  <done>Documents page shows stage-lock message for pre-Build users. API returns 403 for pre-Build users. Both tier and stage gating enforced.</done>
</task>

<task type="auto">
  <name>Task 2: Verify performance and add timeout guard</name>
  <files>app/api/dashboard/deck-review/route.ts</files>
  <action>
  The success criteria requires upload+parse+score within 60 seconds for a 15-slide deck. The existing pipeline is:
  1. PDF parse via pdf-parse (fast, ~1-2s)
  2. AI scoring via `scoreDeck()` which calls `generateTrackedResponse` (single LLM call, typically 10-30s)

  This should already be well within 60s. Add a safety guard:

  1. At the top of the POST handler (after auth/tier/stage checks), wrap the parse+score in a timeout:
     ```typescript
     const controller = new AbortController()
     const timeout = setTimeout(() => controller.abort(), 55000) // 55s to leave room for response
     ```
  2. Pass the signal through if `generateTrackedResponse` supports it, OR just use a Promise.race:
     ```typescript
     const scorecardPromise = (async () => {
       const parsedDeck = await parsePDF(buffer)
       // ... existing text validation ...
       return await scoreDeck(text)
     })()

     const timeoutPromise = new Promise<never>((_, reject) =>
       setTimeout(() => reject(new Error("Scoring timed out")), 55000)
     )

     const scorecard = await Promise.race([scorecardPromise, timeoutPromise])
     ```
  3. Catch the timeout error and return a specific 504 response:
     ```typescript
     { success: false, error: "Deck scoring took too long. Please try a shorter deck or try again later." }
     ```

  Keep the existing code structure intact. Just wrap the parse+score section in Promise.race.
  </action>
  <verify>
  Run `npx tsc --noEmit` to confirm types compile. Review that the timeout is set to 55 seconds (under the 60s requirement with margin for response serialization).
  </verify>
  <done>API has a 55-second timeout guard ensuring the 60-second performance requirement is met, with a user-friendly error message if scoring takes too long.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- Documents page FeatureLock includes `requiredStage="build"`
- API enforces stage-gate check before scoring
- API has 55-second timeout guard
- Pre-Build users see "Continue Your Journey" message on documents page
- Pre-Build users get 403 from deck-review API
</verification>

<success_criteria>
- Feature is double-gated: Pro+ tier AND Build stage
- API cannot be bypassed by pre-Build users
- Performance guard ensures 60-second SLA
- Existing functionality unchanged for Pro+ users who are at Build stage or later
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/87/87-02-SUMMARY.md`
</output>
