---
phase: 87-pitch-deck-scoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - types/deck-review.ts
  - lib/ai/deck-scoring.ts
  - components/dashboard/deck-score-card.tsx
  - app/api/dashboard/deck-review/route.ts
autonomous: true

must_haves:
  truths:
    - "Each of 7 scoring dimensions shows specific 'What VCs want to see' recommendation text"
    - "AI scoring prompt produces vcWantToSee field for every dimension"
    - "Scorecard UI renders VC recommendations alongside suggestions"
    - "API returns vcWantToSee in scorecard response"
  artifacts:
    - path: "types/deck-review.ts"
      provides: "DeckDimension type with vcWantToSee field"
      contains: "vcWantToSee"
    - path: "lib/ai/deck-scoring.ts"
      provides: "Updated DECK_SCORING_PROMPT requesting vcWantToSee"
      contains: "vcWantToSee"
    - path: "components/dashboard/deck-score-card.tsx"
      provides: "VC recommendation display in dimension cards"
      contains: "vcWantToSee"
    - path: "app/api/dashboard/deck-review/route.ts"
      provides: "API endpoint returning updated scorecard shape"
  key_links:
    - from: "lib/ai/deck-scoring.ts"
      to: "types/deck-review.ts"
      via: "import DeckDimension"
      pattern: "vcWantToSee"
    - from: "components/dashboard/deck-score-card.tsx"
      to: "types/deck-review.ts"
      via: "DeckDimension.vcWantToSee"
      pattern: "vcWantToSee"
---

<objective>
Enhance deck scoring to include "What VCs want to see" per dimension.

Purpose: The existing pitch deck scoring pipeline (upload PDF -> parse -> AI score -> display scorecard) is fully built but lacks the "What VCs want to see" recommendation text that the Phase 87 success criteria requires. This plan adds that field to the type, prompt, validation, and UI.

Output: Updated scoring pipeline where each dimension includes a `vcWantToSee` string shown in the scorecard UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@types/deck-review.ts
@lib/ai/deck-scoring.ts
@components/dashboard/deck-score-card.tsx
@app/api/dashboard/deck-review/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add vcWantToSee to type, prompt, and validation</name>
  <files>types/deck-review.ts, lib/ai/deck-scoring.ts</files>
  <action>
  1. In `types/deck-review.ts`, add `vcWantToSee: string` to the `DeckDimension` interface (after `suggestions`). This is a single sentence describing what VCs specifically look for in this dimension.

  2. In `lib/ai/deck-scoring.ts`, update `DECK_SCORING_PROMPT`:
     - Add a `"vcWantToSee"` field to each dimension in the JSON response schema
     - For each of the 7 scoring rubric sections, add a brief note about what VCs specifically want to see, e.g.:
       - Problem Clarity: "VCs want to see a specific, quantified pain point with evidence of real customer suffering"
       - Market Size: "VCs want to see bottom-up TAM calculation with credible sources, not top-down guesses"
       - Team: "VCs want to see domain expertise, prior exits, or clear unfair advantages"
       - Traction: "VCs want to see month-over-month growth metrics, retention data, or signed LOIs"
       - GTM: "VCs want to see tested acquisition channels with known CAC and conversion rates"
       - Narrative: "VCs want to see a compelling story arc that builds urgency and is memorable"
       - Investability: "VCs want to see a clear ask, specific use of funds, and strong unit economics"
     - The AI should customize the vcWantToSee based on the actual deck content, not just return the generic rubric text.

  3. In `lib/ai/deck-scoring.ts` `scoreDeck()`, update the validation in `validatedDimensions` map:
     - Extract `vcWantToSee` from the AI response: `found?.vcWantToSee || ""`
     - Add a fallback based on dimension name if AI doesn't provide one (use the rubric text above as defaults)
  </action>
  <verify>
  Run `npx tsc --noEmit` to confirm types compile. Grep for `vcWantToSee` in `types/deck-review.ts` and `lib/ai/deck-scoring.ts` to confirm presence.
  </verify>
  <done>DeckDimension interface includes vcWantToSee field, AI prompt requests it, scoreDeck() validates and returns it with fallback defaults.</done>
</task>

<task type="auto">
  <name>Task 2: Display VC recommendations in scorecard UI</name>
  <files>components/dashboard/deck-score-card.tsx</files>
  <action>
  In `components/dashboard/deck-score-card.tsx`, update the `DimensionCard` component:

  1. Below the `explanation` paragraph and above the suggestions toggle, add a "What VCs want to see" section:
     ```
     {dimension.vcWantToSee && (
       <div className="mt-2 mb-2 p-2 rounded bg-blue-50 dark:bg-blue-950/30 border border-blue-100 dark:border-blue-900/50">
         <p className="text-xs font-medium text-blue-700 dark:text-blue-300 mb-0.5">
           What VCs want to see
         </p>
         <p className="text-xs text-blue-600 dark:text-blue-400">
           {dimension.vcWantToSee}
         </p>
       </div>
     )}
     ```

  2. The blue styling differentiates it from the orange brand suggestions, making it clear this is VC-specific guidance.

  No other changes needed -- the component already receives the full DeckDimension type.
  </action>
  <verify>
  Run `npx tsc --noEmit` to confirm component compiles. Visually inspect that the "What VCs want to see" block appears in each dimension card by reviewing the code structure.
  </verify>
  <done>Each dimension card in the scorecard UI shows a blue "What VCs want to see" panel with AI-generated VC-perspective feedback.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes with 0 errors
- `grep -r "vcWantToSee" types/ lib/ai/ components/dashboard/` shows presence in all 3 locations
- DeckDimension type includes vcWantToSee: string
- DECK_SCORING_PROMPT includes vcWantToSee in JSON schema
- DeckScoreCard renders vcWantToSee in a visually distinct section
</verification>

<success_criteria>
- Each of 7 scoring categories includes "What VCs want to see" text in both API response and UI display
- Type system enforces vcWantToSee field on DeckDimension
- Backward-compatible (empty string fallback for existing data)
</success_criteria>

<output>
After completion, create `.planning/milestones/v8.0-go-live/phases/87/87-01-SUMMARY.md`
</output>
