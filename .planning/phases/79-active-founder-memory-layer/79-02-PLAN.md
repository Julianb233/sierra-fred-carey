---
phase: 79-active-founder-memory-layer
plan: 02
type: execute
wave: 2
depends_on: ["79-01"]
files_modified:
  - app/api/fred/chat/route.ts
  - lib/fred/context-builder.ts
  - lib/ai/prompt-layers.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Every FRED chat response references at least one founder-specific detail (company name, market, stage, challenge) -- verified by the CRITICAL INSTRUCTION in the system prompt"
    - "New facts mentioned in chat are automatically extracted and stored in semantic memory after each conversation turn (fire-and-forget, non-blocking)"
    - "When memory data is stale (>7 days), the system prompt instructs FRED to ask a re-confirmation question rather than guessing"
    - "When memory data is missing for core fields, the system prompt instructs FRED to ask a clarifying question rather than giving generic advice"
    - "Co-founder field flows through the full pipeline: profile -> context builder -> system prompt -> FRED references it"
  artifacts:
    - path: "app/api/fred/chat/route.ts"
      provides: "Chat route with active memory injection pre-response and memory extraction post-response"
      contains: "buildActiveFounderMemory"
    - path: "lib/fred/context-builder.ts"
      provides: "Updated buildFounderContextWithFacts using active memory layer"
      contains: "formatMemoryBlock"
    - path: "lib/ai/prompt-layers.ts"
      provides: "Core prompt with enhanced FOUNDER SNAPSHOT section referencing active context"
      contains: "ACTIVE FOUNDER CONTEXT"
  key_links:
    - from: "app/api/fred/chat/route.ts"
      to: "lib/fred/active-memory.ts"
      via: "imports buildActiveFounderMemory for pre-chat context loading"
      pattern: "import.*buildActiveFounderMemory.*from.*active-memory"
    - from: "app/api/fred/chat/route.ts"
      to: "lib/fred/active-memory.ts"
      via: "fire-and-forget extractMemoryUpdates + persistMemoryUpdates after response"
      pattern: "extractMemoryUpdates|persistMemoryUpdates"
    - from: "lib/fred/context-builder.ts"
      to: "lib/fred/active-memory.ts"
      via: "buildFounderContextWithFacts delegates to buildActiveFounderMemory + formatMemoryBlock"
      pattern: "buildActiveFounderMemory|formatMemoryBlock"
    - from: "lib/ai/prompt-layers.ts"
      to: "FRED_CORE_PROMPT"
      via: "FOUNDER SNAPSHOT section updated to reinforce mandatory context referencing"
      pattern: "ACTIVE FOUNDER CONTEXT|CRITICAL INSTRUCTION"
---

<objective>
Wire the Active Founder Memory Layer into the FRED chat pipeline and update the context builder. After this plan: (1) every FRED response gets the founder's structured context injected with a mandatory-reference instruction, (2) new facts from each chat turn are extracted and stored automatically, (3) stale/missing context triggers re-confirmation or clarification questions.

Purpose: This is the integration that makes "no generic answers ever" a reality. Plan 01 created the data layer; this plan connects it to the live chat pipeline.

Output: Three modified files -- chat route, context builder, prompt layers.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/phases/79-active-founder-memory-layer/79-01-SUMMARY.md
@app/api/fred/chat/route.ts
@lib/fred/context-builder.ts
@lib/ai/prompt-layers.ts
@lib/fred/active-memory.ts
@lib/fred/founder-memory-types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update context-builder.ts and prompt-layers.ts to use active memory</name>
  <files>
    lib/fred/context-builder.ts
    lib/ai/prompt-layers.ts
  </files>
  <action>
**1. Update `lib/fred/context-builder.ts`:**

Modify `buildFounderContextWithFacts` to integrate the active memory layer. The key change: use `buildActiveFounderMemory` and `formatMemoryBlock` from `@/lib/fred/active-memory` to produce the FOUNDER SNAPSHOT context block, instead of the current manual field-by-field assembly.

Changes:
- Import `buildActiveFounderMemory` and `formatMemoryBlock` from `@/lib/fred/active-memory`
- In `buildFounderContextWithFacts`, after loading profile + facts (keep the existing parallel Promise.all), also call `buildActiveFounderMemory(userId, hasPersistentMemory)` to get the structured FounderMemory
- Replace the call to `buildContextBlock({ profile, facts, isFirstConversation })` with `formatMemoryBlock(founderMemory)` for the core founder context
- KEEP the existing handoff logic (first conversation detection, onboarding handoff instructions) -- append it AFTER the formatMemoryBlock output
- KEEP the existing startup process progress, channel context, and red flags appendages -- they remain unchanged
- KEEP the existing `buildFounderContext` and `buildFounderContextWithFacts` function signatures so all callers remain compatible
- Return both `context` and `facts` as before, plus add `memory` (the FounderMemory struct) to the return type for Plan 02 Task 2 to use

The `buildContextBlock` function can remain in the file but should be marked as deprecated with a comment. Do NOT delete it -- other code paths may use it.

IMPORTANT: Do NOT break backward compatibility. The function signatures and return types must remain compatible. Add the `memory` field as an optional addition to the return object.

**2. Update `lib/ai/prompt-layers.ts`:**

Update the FOUNDER SNAPSHOT section of `FRED_CORE_PROMPT.content` to reinforce the active memory layer. In the `## FOUNDER SNAPSHOT (Context Memory)` section (around line 294 of the current prompt), update the instructions:

Change:
```
## FOUNDER SNAPSHOT (Context Memory)

When founder context is available (injected above), use it to personalize advice. The snapshot may include: stage, product status, traction, runway (time, money, energy), primary constraint, 90-day goal, industry, team size, revenue, funding status, challenges.
```

To:
```
## FOUNDER SNAPSHOT (Active Context Memory)

When the ACTIVE FOUNDER CONTEXT section appears above, it contains this founder's specific details: name, company, stage, market, co-founder, and biggest challenge.

**MANDATORY:** You MUST reference at least one founder-specific detail from the ACTIVE FOUNDER CONTEXT in every substantive response. Use their company name, market, stage, or challenge naturally: "Since [company] is targeting [market]..." or "Given your challenge with [challenge]...". Never give generic startup advice when specific context is available.

**When context is stale:** If the ACTIVE FOUNDER CONTEXT flags stale fields, naturally confirm them: "Last time we spoke, you mentioned [X]. Is that still the case?" Do this ONCE per stale field, not every turn.

**When context is missing:** If core fields are missing, ask about them naturally within the first 2-3 exchanges. Do NOT block the founder from getting value -- weave collection into the conversation.
```

IMPORTANT: The `FRED_CORE_PROMPT` is frozen with `Object.freeze`. You need to update the template literal string that defines `content` -- this is a source code change, not a runtime mutation. The `Object.freeze` prevents runtime modification but the source code can be updated. Update the `version` to `'1.1.0'` and `lastModified` to `'2026-03-08'`.

Also update the `## UNIVERSAL ENTRY FLOW (First Interaction)` section to include co-founder as one of the Business Fundamentals to collect:

Add to the Business Fundamentals Collection Protocol list (after item 6 "Funding stage"):
```
7. **Co-founder status** -- "Are you doing this solo or do you have a co-founder?"
```

And update the "Rules for collecting fundamentals" to note: "7 fundamentals total. Weave into 2-3 exchanges naturally."
  </action>
  <verify>
- `npm run build` passes (this is the critical check since prompt-layers.ts is imported everywhere)
- The FRED_CORE_PROMPT.content now contains "ACTIVE FOUNDER CONTEXT" and "MANDATORY" reference instruction
- The co-founder collection question is in the Business Fundamentals list
- `grep -n "ACTIVE FOUNDER CONTEXT" lib/ai/prompt-layers.ts` returns matches
- `grep -n "co-founder" lib/ai/prompt-layers.ts` returns matches in the fundamentals section
- `npm test -- --run` passes (particularly lib/ai/__tests__/prompts.test.ts)
  </verify>
  <done>
- context-builder.ts uses active memory layer to produce founder context block
- Backward compatibility maintained -- same function signatures and return types
- FRED_CORE_PROMPT updated with mandatory context-referencing instruction
- Co-founder added to Business Fundamentals Collection Protocol
- Stale/missing field handling instructions embedded in core prompt
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire memory extraction into chat route (post-response auto-refresh)</name>
  <files>app/api/fred/chat/route.ts</files>
  <action>
Modify the FRED chat route (`app/api/fred/chat/route.ts`) to add post-response memory extraction. This is the "auto-refresh" mechanism: after each chat turn, extract new facts from the conversation and store them.

**Pre-response (context injection):**
The context injection already works via `buildFounderContextWithFacts` which now (after Task 1) delegates to `buildActiveFounderMemory` + `formatMemoryBlock`. No changes needed for the pre-response path -- it already injects founder context into the system prompt via `buildSystemPrompt(founderContext)`.

**Post-response (memory extraction):**
Add a fire-and-forget memory extraction step that runs AFTER the streaming response completes. This should be placed alongside the existing fire-and-forget operations (profile enrichment, next steps extraction, journey event logging).

1. Import `extractMemoryUpdates` and `persistMemoryUpdates` from `@/lib/fred/active-memory`

2. Find the existing fire-and-forget block in the chat route. It currently does:
   - Profile enrichment (`extractProfileEnrichment` + DB update)
   - Next steps extraction (`extractAndStoreNextSteps`)
   - Journey event logging (`logJourneyEventAsync`)
   - Feedback signal insertion
   - Episode storage (`storeEpisode`)

3. Add a new fire-and-forget call alongside these (NOT blocking the response):
```typescript
// Phase 79: Active Founder Memory -- extract and persist new facts
;(async () => {
  try {
    const updates = await extractMemoryUpdates(
      userMessage,      // the user's message content
      assistantText     // the accumulated assistant response text
    )
    if (updates.length > 0) {
      await persistMemoryUpdates(userId, updates)
    }
  } catch (error) {
    console.warn('[FRED Chat] Memory extraction failed (non-blocking):', error)
  }
})()
```

4. This should run for ALL tiers (not just Pro+). Even free-tier users benefit from memory updates to their profile columns. The semantic memory storage inside `persistMemoryUpdates` should check `hasPersistentMemory` before writing to semantic facts -- pass this as a parameter or have `persistMemoryUpdates` always write to profile columns but conditionally write to semantic memory.

   Update `persistMemoryUpdates` signature if needed: `persistMemoryUpdates(userId: string, updates: MemoryUpdate[], hasPersistentMemory: boolean)`. The function should:
   - ALWAYS update profile columns (name, company_name, co_founder, stage, industry) for ALL tiers
   - Only store semantic facts (via storeFact) when hasPersistentMemory is true

5. The extraction runs on the FULL user message + assistant response text from the current turn, not the entire conversation history. This keeps the extraction fast and focused.

IMPORTANT considerations:
- Do NOT modify the streaming response path. The memory extraction is strictly fire-and-forget AFTER the response completes.
- Do NOT add any new blocking operations. The chat response time must not increase.
- The existing `extractProfileEnrichment` (heuristic regex-based) continues to run alongside the new LLM-based extraction. They are complementary: regex catches structured patterns (MRR, team size numbers), LLM catches semantic facts (co-founder mentioned by name, market pivot).
- Use the same pattern as existing fire-and-forget operations in the route (wrapped in IIFE with try/catch, console.warn on failure).
  </action>
  <verify>
- `npm run build` passes
- `grep -n "extractMemoryUpdates" app/api/fred/chat/route.ts` returns the import and usage
- `grep -n "persistMemoryUpdates" app/api/fred/chat/route.ts` returns the usage
- The memory extraction is inside an async IIFE (fire-and-forget pattern)
- `npm test -- --run` passes
- The streaming response path is NOT modified (no new await before response)
  </verify>
  <done>
- Post-response memory extraction fires after every FRED chat turn
- New facts are extracted via LLM and stored in profile columns + semantic memory
- Extraction is fire-and-forget -- never blocks or slows the chat response
- Works for all tiers (profile updates for everyone, semantic facts for Pro+)
- Complements existing regex-based enrichment extractor
- Every success criterion from the phase is met:
  1. FRED references founder-specific details (via CRITICAL INSTRUCTION in prompt)
  2. Memory auto-refreshed after each conversation (via post-response extraction)
  3. Co-founder field captured and available to FRED (via profile column + context builder)
  4. Stale/missing data triggers clarification questions (via formatMemoryBlock instructions)
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `npm test -- --run` passes with no regressions
3. Grep verification:
   - `grep -n "ACTIVE FOUNDER CONTEXT" lib/ai/prompt-layers.ts` -- confirms prompt update
   - `grep -n "extractMemoryUpdates" app/api/fred/chat/route.ts` -- confirms extraction wiring
   - `grep -n "buildActiveFounderMemory" lib/fred/context-builder.ts` -- confirms context builder integration
   - `grep -n "co-founder\|co_founder" lib/ai/prompt-layers.ts` -- confirms co-founder in fundamentals
4. The FRED_CORE_PROMPT contains the mandatory context-referencing instruction
5. No backward compatibility breaks in function signatures
</verification>

<success_criteria>
1. Every FRED chat response gets the ACTIVE FOUNDER CONTEXT block injected into the system prompt, containing all known founder-specific details with a CRITICAL INSTRUCTION to reference them
2. After each chat turn, new facts mentioned by the founder are extracted via LLM and stored (fire-and-forget, non-blocking)
3. Co-founder field flows end-to-end: profile column -> context builder -> active memory -> system prompt -> FRED references it
4. Stale fields (>7 days old) are flagged in the prompt with re-confirmation instructions for FRED
5. Missing fields are flagged with natural collection instructions -- FRED asks, never guesses
6. `npm run build` and `npm test -- --run` pass with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/79-active-founder-memory-layer/79-02-SUMMARY.md`
</output>
