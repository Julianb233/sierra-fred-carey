---
phase: 78-oases-stage-visualization
plan: 02
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - components/oases/oases-visualizer.tsx
  - components/oases/stage-detail-modal.tsx
  - components/tier/feature-lock.tsx
  - hooks/use-oases-progress.ts
  - app/dashboard/page.tsx
  - app/dashboard/layout.tsx
autonomous: false

must_haves:
  truths:
    - "User sees a horizontal desert-themed 5-stage timeline on the dashboard below the hero section"
    - "Current stage is highlighted with animation; completed stages show checkmarks; locked stages are dimmed"
    - "Clicking a stage opens a modal showing that stage's requirements and completion status"
    - "Overall journey percentage is displayed prominently and updates when steps are completed"
    - "Gated routes show a lock overlay with 'Complete [stage] first' messaging instead of content"
    - "Boardy/fund matching is grayed out with journey completion percentage until 100%"
    - "Visualization is responsive — horizontal scroll on mobile, full display on desktop"
  artifacts:
    - path: "components/oases/oases-visualizer.tsx"
      provides: "Desert-themed horizontal timeline showing 5 Oases stages"
      min_lines: 100
    - path: "components/oases/stage-detail-modal.tsx"
      provides: "Modal showing stage requirements and completion checklist"
      min_lines: 50
    - path: "components/tier/feature-lock.tsx"
      provides: "Extended FeatureLock with requiredStage prop for stage-based gating"
      contains: "requiredStage"
    - path: "hooks/use-oases-progress.ts"
      provides: "Client-side hook fetching Oases progress from API"
      exports: ["useOasesProgress"]
    - path: "app/dashboard/page.tsx"
      provides: "Dashboard with OasesVisualizer integrated below hero"
      contains: "OasesVisualizer"
  key_links:
    - from: "components/oases/oases-visualizer.tsx"
      to: "hooks/use-oases-progress.ts"
      via: "useOasesProgress hook"
      pattern: "useOasesProgress"
    - from: "hooks/use-oases-progress.ts"
      to: "/api/oases/progress"
      via: "fetch call"
      pattern: "fetch.*api/oases/progress"
    - from: "components/tier/feature-lock.tsx"
      to: "lib/oases/stage-config.ts"
      via: "stage gate check"
      pattern: "requiredStage"
    - from: "app/dashboard/page.tsx"
      to: "components/oases/oases-visualizer.tsx"
      via: "component import"
      pattern: "OasesVisualizer"
---

<objective>
Build the desert-themed Oases stage visualization component, extend FeatureLock with stage-based gating, and integrate the visualizer into the dashboard.

Purpose: This is the user-facing half of Phase 78 — founders see their venture journey as a visual 5-stage desert roadmap and understand exactly where they are. Stage-gating prevents skipping ahead.

Output: OasesVisualizer component on dashboard, stage detail modal, stage-aware FeatureLock, client-side progress hook.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/STATE.md
@.planning/phases/78-oases-stage-visualization/78-01-SUMMARY.md
@components/tier/feature-lock.tsx
@app/dashboard/page.tsx
@app/dashboard/layout.tsx
@types/oases.ts
@lib/oases/stage-config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client Hook + OasesVisualizer + Stage Detail Modal</name>
  <files>
    hooks/use-oases-progress.ts
    components/oases/oases-visualizer.tsx
    components/oases/stage-detail-modal.tsx
  </files>
  <action>
**Client Hook** (`hooks/use-oases-progress.ts`):

```typescript
"use client";
import { useState, useEffect, useCallback } from "react";
import type { OasesProgress } from "@/types/oases";

export function useOasesProgress() {
  const [progress, setProgress] = useState<OasesProgress | null>(null);
  const [isLoading, setIsLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const refresh = useCallback(async () => {
    try {
      const res = await fetch("/api/oases/progress");
      if (!res.ok) throw new Error("Failed to load progress");
      const data = await res.json();
      if (data.success) setProgress(data.data);
    } catch (err) {
      setError(err instanceof Error ? err.message : "Unknown error");
    } finally {
      setIsLoading(false);
    }
  }, []);

  useEffect(() => { refresh(); }, [refresh]);

  return { progress, isLoading, error, refresh };
}
```

**OasesVisualizer** (`components/oases/oases-visualizer.tsx`):

"use client" component. Import types from `@/types/oases`, hook from `@/hooks/use-oases-progress`, Framer Motion for animations, Lucide icons (Compass, Search, Hammer, Rocket, TrendingUp, Check, Lock, ChevronRight).

Design specifications:
- Full-width card with desert-gradient background: `bg-gradient-to-r from-amber-50 via-orange-50 to-amber-100 dark:from-amber-950/30 dark:via-orange-950/20 dark:to-amber-950/30`
- Border: `border border-[#ff6a1a]/20 rounded-xl`
- Header row: "Your Venture Journey" title (left) + journey percentage badge (right)
  - Percentage badge: circular progress ring using SVG (radius 20, strokeWidth 3), filled with `#ff6a1a`, text centered showing `XX%`
- 5 stage nodes connected by a horizontal line (dashed for locked segments, solid for completed)
- Each node is a circle (48x48px desktop, 40x40px mobile) containing the stage icon
  - Completed: `bg-green-500 text-white` with Check icon overlay
  - Current: `bg-[#ff6a1a] text-white` with pulsing ring animation (Framer Motion `animate={{ scale: [1, 1.1, 1] }}` with `repeat: Infinity`, `duration: 2`)
  - Locked: `bg-gray-200 dark:bg-gray-700 text-gray-400` with Lock icon
- Below each node: stage name (bold) + tagline (small muted text)
- Connecting line between nodes: use a flex row with `flex-1 h-0.5` dividers between nodes
  - Completed segments: `bg-green-500`
  - Current segment (leading to current): `bg-gradient-to-r from-green-500 to-[#ff6a1a]`
  - Locked segments: `bg-gray-200 dark:bg-gray-700 border-dashed`
- Each stage node is `cursor-pointer` and calls `onStageClick(stageId)` to open the detail modal
- Below the timeline: a subtle progress bar spanning full width showing overall journey percentage

Mobile responsive behavior:
- On mobile (< md), the stage nodes scroll horizontally within an `overflow-x-auto` container
- Stage names truncate to first word on small screens
- Minimum width per stage node: 80px with gap-2
- The container has `pb-2` for scrollbar space and `-mx-4 px-4` for edge-to-edge feel

The component accepts an optional `onStageClick?: (stageId: OasesStage) => void` prop. Internally, manage a `selectedStage` state and render `<StageDetailModal>` when a stage is clicked.

Loading state: Show 5 skeleton circles with skeleton lines between them (same layout, animated pulse).

Icon mapping — create a const map inside the component:
```typescript
const STAGE_ICONS: Record<string, React.ComponentType<{ className?: string }>> = {
  Compass, Search, Hammer, Rocket, TrendingUp,
};
```
Use the icon name from StageConfig to look up the component.

**Stage Detail Modal** (`components/oases/stage-detail-modal.tsx`):

"use client" component. Uses `Dialog` from `@/components/ui/dialog` (shadcn). Import types from `@/types/oases`.

Props:
```typescript
interface StageDetailModalProps {
  stage: OasesProgress["stages"][number] & { config: StageConfig } | null;
  open: boolean;
  onOpenChange: (open: boolean) => void;
}
```

Content:
- Dialog title: stage name + tagline
- Stage description paragraph
- Checklist of steps:
  - Each step shows: checkbox icon (CheckCircle2 green if completed, Circle gray if not) + step label + step description
  - Completed steps have `line-through` on label, green text
  - Incomplete steps show what's needed (description)
- Footer shows: "X of Y steps completed" + progress bar
- If all steps complete and this is the current stage, show an "Advance to [Next Stage]" button (calls POST /api/oases/advance, then refreshes progress)
- If this stage is locked, show a message: "Complete [previous stage name] first to unlock this stage"

To get the full StageConfig for the modal, import `STAGE_CONFIG` from `@/lib/oases/stage-config` and match by stage id.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Read back all 3 files and verify: hook fetches from `/api/oases/progress`, visualizer renders 5 nodes with correct states, modal shows checklist with advance button.
  </verify>
  <done>
OasesVisualizer renders a desert-themed horizontal 5-stage timeline with animated current stage, checkmarks on completed stages, locks on future stages. StageDetailModal shows requirements checklist with advance capability. useOasesProgress hook provides real-time progress data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend FeatureLock + Integrate into Dashboard</name>
  <files>
    components/tier/feature-lock.tsx
    app/dashboard/page.tsx
  </files>
  <action>
**Extend FeatureLock** (`components/tier/feature-lock.tsx`):

Read the existing file first. Add a new optional prop `requiredStage?: OasesStage` and `currentStage?: OasesStage` to `FeatureLockProps`. Import `OasesStage` from `@/types/oases` and `STAGE_ORDER, getStageConfig` from `@/lib/oases/stage-config`.

Modify the `FeatureLock` component:
- Add stage check: if `requiredStage` and `currentStage` are both provided, check if `STAGE_ORDER.indexOf(currentStage) >= STAGE_ORDER.indexOf(requiredStage)`. If not, the user does NOT have access regardless of tier.
- The existing tier check remains — both tier AND stage must pass (if both are specified).
- When blocked by stage (not tier), show a different overlay message:
  - Icon: a desert/journey themed icon (Map from lucide instead of Lock)
  - Title: "Continue Your Journey"
  - Description: "Complete the **{required stage name}** stage to unlock {featureName}. You're currently in **{current stage name}**."
  - Instead of UpgradeTier button, show a Link to "/dashboard" with text "View Your Journey" styled as a button with `bg-[#ff6a1a]` colors
- When blocked by tier (not stage), show the existing tier-based lock overlay (no changes)
- When blocked by both, show the stage message (stage takes priority — it's more actionable)

Do the same for `InlineFeatureLock` — add optional `requiredStage` / `currentStage` props with a compact inline message.

**Integrate OasesVisualizer into Dashboard** (`app/dashboard/page.tsx`):

Read the existing file first. This file has pre-commit hooks that auto-revert changes, so be careful. However, per the MEMORY.md note, this applies to `dashboard/layout.tsx` and `documents/new/page.tsx`, NOT `dashboard/page.tsx`. So direct modification is safe.

Add the OasesVisualizer below the `FredHero` component and above the main content grid. The integration point:

1. Import `OasesVisualizer` from `@/components/oases/oases-visualizer`
2. In the `DashboardContent` component's JSX, after the `<FredHero>` section and before the grid of cards, add:
   ```tsx
   {/* Oases Venture Journey */}
   <FadeIn delay={0.1}>
     <OasesVisualizer />
   </FadeIn>
   ```
3. Wrap it in the existing `FadeIn` animation component for consistency with the rest of the dashboard

The OasesVisualizer is self-contained (fetches its own data via useOasesProgress hook), so no additional data fetching is needed in the dashboard page.

Do NOT modify the dashboard layout.tsx file (pre-commit hooks protect it). The visualizer integration goes in page.tsx only.

Note: The existing `app/dashboard/layout.tsx` already has conditional nav items based on tier and stage. The Oases stage-gating in FeatureLock is for page-level content gating, separate from nav visibility. Phase 80 (Structured Stage-Gate Enforcement) will wire the layout nav to use Oases stages; for now, just get the visualization and FeatureLock extension working.
  </action>
  <verify>
Run `npx tsc --noEmit` — no type errors. Run `npm run build` to confirm the build succeeds with the new components. Read back `components/tier/feature-lock.tsx` and confirm `requiredStage` prop exists. Read back `app/dashboard/page.tsx` and confirm `OasesVisualizer` is imported and rendered.
  </verify>
  <done>
FeatureLock supports both `requiredTier` and `requiredStage` props — stage-blocked features show a journey-themed lock overlay with "View Your Journey" CTA. OasesVisualizer is prominently displayed on the dashboard below the hero section. The desert-themed 5-stage timeline renders with real-time progress data.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Desert-themed Oases stage visualization on dashboard with 5-stage horizontal timeline (Clarity, Validation, Build, Launch, Grow), stage detail modal with requirements checklist, stage-aware FeatureLock component, and journey completion percentage display.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000/dashboard
    2. Verify the Oases timeline appears below the hero section with 5 stages
    3. Current stage (Clarity for new users) should have a pulsing orange ring
    4. Click on each stage — a modal should show the requirements checklist
    5. Locked stages should appear dimmed with lock icons
    6. Journey percentage should display (likely 0% for a fresh account)
    7. Check mobile view (resize to ~375px width) — timeline should scroll horizontally
    8. Verify the overall desert theme matches Sahara branding (amber/orange gradients)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. `npm run build` succeeds
3. OasesVisualizer renders 5 stage nodes with correct visual states (completed/current/locked)
4. StageDetailModal shows requirements checklist with completion status
5. FeatureLock accepts requiredStage prop and shows stage-specific lock overlay
6. Journey percentage displays and updates
7. Dashboard page renders OasesVisualizer below hero section
8. Mobile responsive: horizontal scroll on small screens
</verification>

<success_criteria>
- Dashboard displays Sahara-themed horizontal timeline showing 5 Oases stages
- Each stage is clickable and shows requirements in a modal
- Current stage has animated highlight; completed stages show checkmarks; locked stages are dimmed
- Journey completion percentage is prominently displayed
- FeatureLock extended with requiredStage prop for stage-based gating
- Stage-blocked content shows "Continue Your Journey" overlay with link to dashboard
- Responsive on mobile with horizontal scroll
- TypeScript compiles and build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/78-oases-stage-visualization/78-02-SUMMARY.md`
</output>
