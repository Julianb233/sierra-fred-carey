---
phase: 73-admin-dashboard-sentiment
plan: 04
type: execute
wave: 2
depends_on: ["73-02"]
files_modified:
  - app/api/admin/feedback/export/route.ts
  - app/api/cron/feedback-digest/route.ts
  - lib/email/templates/feedback-digest.tsx
  - lib/db/feedback-admin.ts
autonomous: true

must_haves:
  truths:
    - "Admin can download a CSV of filtered feedback data"
    - "Weekly digest email arrives in admin inbox with feedback summary"
    - "CSV contains all relevant columns: date, channel, type, rating, category, tier, sentiment, comment, user"
    - "Digest email shows top issues, flagged sessions, and volume trends"
  artifacts:
    - path: "app/api/admin/feedback/export/route.ts"
      provides: "CSV export endpoint for feedback data"
      exports: ["GET"]
    - path: "app/api/cron/feedback-digest/route.ts"
      provides: "Cron endpoint for weekly feedback digest email"
      exports: ["GET"]
    - path: "lib/email/templates/feedback-digest.tsx"
      provides: "React Email template for weekly feedback digest"
  key_links:
    - from: "app/admin/feedback/page.tsx"
      to: "/api/admin/feedback/export"
      via: "Download CSV button with current filters"
      pattern: "api/admin/feedback/export"
    - from: "app/api/cron/feedback-digest/route.ts"
      to: "lib/email/send.ts"
      via: "sendEmail with FeedbackDigest template"
      pattern: "sendEmail"
    - from: "app/api/cron/feedback-digest/route.ts"
      to: "lib/db/feedback-admin.ts"
      via: "getFeedbackStats for digest data"
      pattern: "getFeedbackStats"
---

<objective>
Add CSV export for feedback data and a weekly feedback digest email to the admin.

Purpose: REQ-V6, REQ-V7 -- admin needs offline analysis capability via CSV and a proactive weekly summary email so they don't have to check the dashboard constantly.

Output: CSV export API endpoint, cron-triggered digest email endpoint, and React Email template.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md (REQ-V6, REQ-V7)
@lib/email/send.ts (sendEmail pattern)
@lib/email/client.ts (getResendClient)
@app/api/cron/weekly-digest/route.ts (pattern for cron + auth + email sending)
@lib/db/feedback-admin.ts (queryFeedbackSignalsAdmin, getFeedbackStats from plan 73-02)
@lib/feedback/types.ts
@lib/feedback/constants.ts
@lib/auth/admin.ts (requireAdminRequest)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CSV export endpoint</name>
  <files>app/api/admin/feedback/export/route.ts, app/admin/feedback/page.tsx</files>
  <action>
Create `app/api/admin/feedback/export/route.ts`:

1. GET handler:
   - `requireAdminRequest(request)` -- return 401 if denied.
   - Parse the same filter params as the list endpoint (dateFrom, dateTo, channel, rating, category, tier, userId).
   - Call `queryFeedbackSignalsAdmin(filters)` with limit=10000 (export all matching, not paginated).
   - Convert to CSV:
     - Header row: "Date,Channel,Signal Type,Rating,Category,Tier,Sentiment Score,Sentiment Confidence,Comment,User ID,Session ID"
     - For each signal, format a CSV row. Escape commas and quotes in comment field (wrap in double quotes, escape internal quotes).
     - Use a simple manual CSV builder -- do NOT add a CSV library dependency.
   - Return with headers:
     ```
     Content-Type: text/csv
     Content-Disposition: attachment; filename="feedback-export-{date}.csv"
     ```
   - The `{date}` should be today's date in YYYY-MM-DD format.

2. Update `app/admin/feedback/page.tsx`:
   - Add a "Export CSV" button in the filters bar (next to "Apply Filters").
   - On click, construct the URL `/api/admin/feedback/export` with the current filter params as query string.
   - Open the URL in a new tab (`window.open(url, '_blank')`) -- the browser will download the CSV automatically from the Content-Disposition header.
   - Style the button as a secondary/outline button to distinguish from the primary filter button.
  </action>
  <verify>
  Run `npx tsc --noEmit`. Click the "Export CSV" button in the admin feedback page -- it should trigger a CSV download. Open the CSV in a text editor and verify the header and data rows are properly formatted.
  </verify>
  <done>
  - CSV export endpoint returns properly formatted CSV with all feedback columns
  - Filters are passed through to the export query
  - "Export CSV" button appears on the admin feedback page
  - Downloaded file has correct filename with date
  </done>
</task>

<task type="auto">
  <name>Task 2: Create weekly feedback digest email and cron endpoint</name>
  <files>lib/email/templates/feedback-digest.tsx, app/api/cron/feedback-digest/route.ts, lib/db/feedback-admin.ts</files>
  <action>
Add to `lib/db/feedback-admin.ts`:

1. Export `getDigestSummary(daysBack: number = 7)`:
   - Compute dateFrom = 7 days ago, dateTo = now.
   - Call `getFeedbackStats(dateFrom, dateTo)`.
   - Query flagged sessions from last 7 days: `feedback_sessions` where `flagged=true` and `created_at >= dateFrom`.
   - Query top categories (top 5 by count).
   - Return `{ stats, flaggedSessions: FeedbackSession[], topCategories: Array<{ category: string, count: number }>, period: { from: string, to: string } }`.

Create `lib/email/templates/feedback-digest.tsx`:

1. React Email component `FeedbackDigest` accepting props:
   ```ts
   interface FeedbackDigestProps {
     totalSignals: number
     positiveCount: number
     negativeCount: number
     flaggedSessionCount: number
     topCategories: Array<{ category: string; count: number }>
     dailyVolume: Array<{ date: string; count: number }>
     period: { from: string; to: string }
   }
   ```

2. Template layout (follow the pattern of existing email templates like `WeeklyDigest`):
   - Header: "Sahara Feedback Digest" with Sahara Orange accent.
   - Period: "Week of {from} to {to}".
   - Summary section:
     - Total feedback signals received
     - Positive/Negative ratio
     - Flagged sessions count (with warning color if > 0)
   - Top categories section:
     - List of top 5 categories with counts
     - "coaching_discomfort" shown with note: "(working as designed)"
   - Daily volume section:
     - Simple text-based sparkline or list of daily counts
   - CTA button: "View Full Dashboard" linking to the admin feedback page URL.
   - Footer: "This is an automated weekly digest from Sahara."

3. Use React Email components: `Html`, `Head`, `Body`, `Container`, `Section`, `Text`, `Button`, `Hr`.
   - Import from `@react-email/components` (check if this package exists in the project, otherwise use raw JSX with inline styles like other email templates).

Create `app/api/cron/feedback-digest/route.ts`:

1. Follow the EXACT pattern of `app/api/cron/weekly-digest/route.ts`:
   - `export const dynamic = 'force-dynamic'`
   - `export const maxDuration = 60`
   - GET handler with CRON_SECRET auth check (timing-safe comparison using the same HMAC pattern).
   - Call `getDigestSummary(7)` to get the data.
   - Get admin email from `process.env.ADMIN_EMAIL` (fallback to `process.env.ADMIN_FEEDBACK_EMAIL`).
   - If no admin email configured, log warning and return 200 with `{ skipped: true, reason: 'No admin email configured' }`.
   - Call `sendEmail()` from `@/lib/email/send` with:
     - `to`: admin email
     - `subject`: `"Sahara Feedback Digest: {from} - {to}"`
     - `react`: `<FeedbackDigest {...digestData} />`
     - `tags`: `[{ name: 'category', value: 'feedback-digest' }]`
   - Return JSON `{ success: true, signalCount: stats.totalSignals }`.

2. Add vercel.json cron config note in a comment:
   ```ts
   // Vercel Cron: Add to vercel.json crons array:
   // { "path": "/api/cron/feedback-digest", "schedule": "0 10 * * 1" }
   // Runs every Monday at 10:00 AM UTC (same as weekly-digest)
   ```
  </action>
  <verify>
  Run `npx tsc --noEmit`. Run `npm run build` to verify all files compile. Check that the email template renders valid JSX. Verify the cron endpoint follows the same auth pattern as the existing weekly-digest route.
  </verify>
  <done>
  - Weekly feedback digest email template renders with all summary sections
  - Cron endpoint authenticates with CRON_SECRET and sends digest via Resend
  - Digest includes: total signals, pos/neg ratio, flagged sessions, top categories, daily volume
  - Admin email is configurable via ADMIN_EMAIL env var
  - Vercel cron schedule documented in comments
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- CSV export endpoint returns valid CSV with proper headers
- Email template compiles and renders
- Cron endpoint follows existing auth pattern
- Export CSV button visible on admin feedback page
</verification>

<success_criteria>
- REQ-V6: CSV export downloads feedback data with all relevant columns and current filters
- REQ-V7: Weekly feedback digest email sent to admin via Resend with volume, trends, flagged sessions
</success_criteria>

<output>
After completion, create `.planning/phases/73-admin-dashboard-sentiment/73-04-SUMMARY.md`
</output>
