---
phase: 82-chat-voice-continuity
plan: 02
type: execute
wave: 1
depends_on: ["82-01"]
files_modified:
  - app/api/livekit/webhook/route.ts
  - lib/fred/chat-voice-bridge.ts
  - components/chat/voice-call-context-banner.tsx
  - components/chat/fred-chat.tsx
autonomous: true

must_haves:
  truths:
    - "After a voice call ends, the transcript and summary are injected back into the chat history"
    - "Before a voice call starts, the user sees 'Last discussed: [topic]' so they know FRED remembers"
    - "Switching between text and voice on mobile feels seamless with no context loss"
  artifacts:
    - path: "app/api/livekit/webhook/route.ts"
      provides: "Webhook handler that stores voice transcript into chat history on room_finished"
      contains: "storeChannelEntry"
    - path: "components/chat/voice-call-context-banner.tsx"
      provides: "Pre-call banner showing last discussed topic and post-call transcript summary"
      exports: ["VoiceCallContextBanner"]
    - path: "lib/fred/chat-voice-bridge.ts"
      provides: "Voice-to-chat transcript injection utility"
      exports: ["injectVoiceTranscriptToChat"]
  key_links:
    - from: "app/api/livekit/webhook/route.ts"
      to: "lib/fred/chat-voice-bridge.ts"
      via: "injectVoiceTranscriptToChat on room_finished"
      pattern: "injectVoiceTranscriptToChat"
    - from: "components/chat/voice-call-context-banner.tsx"
      to: "/api/fred/call"
      via: "displays lastDiscussedTopic from call API response"
      pattern: "lastDiscussedTopic"
---

<objective>
Complete the bidirectional context bridge: inject voice transcripts back into chat history after calls end, and show context awareness UI before/after calls.

Purpose: After a voice call, the conversation should appear in the chat timeline so the founder sees one continuous thread. Before a call, they should see what FRED remembers. This completes the seamless text/voice switching experience.

Output: Updated webhook to store voice transcripts as chat entries, a context banner component, and integration into the chat UI.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v8.0-go-live/ROADMAP.md
@.planning/STATE.md
@.planning/phases/82-chat-voice-continuity/82-01-SUMMARY.md
@app/api/livekit/webhook/route.ts
@lib/channels/conversation-context.ts
@lib/fred/chat-voice-bridge.ts
@components/chat/voice-chat-overlay.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Store voice transcripts in chat history via webhook</name>
  <files>app/api/livekit/webhook/route.ts, lib/fred/chat-voice-bridge.ts</files>
  <action>
Add a new exported function to `lib/fred/chat-voice-bridge.ts`:

`injectVoiceTranscriptToChat(userId: string, roomName: string, transcript: Array<{speaker: 'user' | 'fred', text: string, timestamp: string}>): Promise<void>`:
   - Import `storeChannelEntry` from `@/lib/channels/conversation-context`
   - Generate a unique sessionId for this voice call using the roomName (e.g., `voice-${roomName}`)
   - For each transcript entry:
     - Map speaker 'user' to role 'user' and 'fred' to role 'assistant'
     - Call `storeChannelEntry(userId, sessionId, 'voice', role, text, { roomName, voiceTimestamp: timestamp })`
   - After storing individual entries, create one summary entry:
     - Concatenate all user messages into a brief summary (first 200 chars)
     - Store as assistant role with content: `[Voice Call Summary] You called and discussed: ${summary}. FRED covered: ${assistantSummary}`
     - Tag with channel: 'voice' and metadata: `{ type: 'voice_summary', roomName }`
   - Wrap everything in try/catch — log warnings on failure, never throw

Then update `app/api/livekit/webhook/route.ts`:

In the `room_finished` case, after existing logic:
   - Only proceed for fred-call rooms: `if (roomName.includes('fred-call'))`
   - Extract userId using existing `extractUserIdFromRoom(roomName)` function
   - If userId exists, fetch the transcript from the `coaching_sessions` table (check if a `transcript` column exists) or from the data channel messages stored during the call
   - Since the voice agent publishes transcript data via LiveKit data channel (see agent.ts `publishTranscript`), and those are real-time, we need a different approach:
     - Add a new Supabase table query: check if `coaching_sessions` has a `transcript` JSONB column. If not, the transcript data must be collected differently.
     - The simplest approach: In the webhook's `participant_left` handler for agent participants, collect any stored transcript. But since data channel messages are ephemeral, the better approach is:
     - Create a lightweight `/api/fred/call/transcript` endpoint that the client calls after the call ends, passing the collected transcript data (the client already receives data channel messages in real-time via LiveKit)
   - REVISED APPROACH (simpler, no new table columns needed):
     - In the `room_finished` webhook handler, after updating coaching_sessions:
     - Fetch recent `fred_episodic_memory` entries for this user with channel='voice' from the last 30 minutes
     - If none exist (transcript not yet stored), that's OK — the client-side will handle it
   - Add a comment explaining that transcript injection happens client-side (the LiveKit React components receive data channel messages and can POST them)

Add a new API route concept (but implement it in this file for simplicity as a helper):
   - Export a new function `storeVoiceCallTranscript` that the client can call via a new small API endpoint
   - Actually, add a PATCH handler to `app/api/fred/call/route.ts` (from Plan 01's modified file) that accepts `{ roomName, transcript: Array<{speaker, text, timestamp}> }` and calls `injectVoiceTranscriptToChat`

WAIT — simpler approach. Do this instead:

1. In `lib/fred/chat-voice-bridge.ts`, add `injectVoiceTranscriptToChat` as described above.

2. Create `app/api/fred/call/transcript/route.ts` as a new POST endpoint:
   - Requires auth via `requireAuth()`
   - Accepts `{ roomName: string, transcript: Array<{speaker: 'user' | 'fred', text: string, timestamp: string}> }`
   - Validates that the roomName contains the userId (security check)
   - Calls `injectVoiceTranscriptToChat(userId, roomName, transcript)`
   - Returns `{ success: true }`

3. In `app/api/livekit/webhook/route.ts`, in the `room_finished` case for fred-call rooms:
   - Add a comment: "Transcript injection is handled client-side via POST /api/fred/call/transcript"
   - No actual transcript logic needed in the webhook — keep it clean

This approach is cleanest because the client already receives real-time transcript data via LiveKit's data channel (the agent.ts `publishTranscript` function sends it), so the client has the full transcript and can POST it when the call ends.
  </action>
  <verify>
    Run `npm run build` to verify all new files compile.
    Verify the new route exists: `ls app/api/fred/call/transcript/route.ts`
  </verify>
  <done>
    - `injectVoiceTranscriptToChat` function stores voice transcript entries as episodic memory with channel='voice'
    - POST `/api/fred/call/transcript` endpoint accepts and stores transcripts
    - Transcript includes both individual entries and a summary entry
    - Build passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pre-call context banner and wire into chat UI</name>
  <files>components/chat/voice-call-context-banner.tsx, components/chat/fred-chat.tsx</files>
  <action>
Create `components/chat/voice-call-context-banner.tsx`:

A small React component that shows context awareness before/after voice calls.

Props:
```typescript
interface VoiceCallContextBannerProps {
  lastDiscussedTopic: string | null;
  isCallActive: boolean;
  lastCallSummary: string | null;
  onDismiss?: () => void;
}
```

Render logic:
- **Pre-call state** (lastDiscussedTopic exists, no active call):
  - Show a subtle banner: "FRED remembers: Last discussed — [topic]"
  - Use Sahara orange accent (#ff6a1a), small text, with a Phone icon from lucide-react
  - Animate in with framer-motion (fade + slide from top, 200ms)

- **Post-call state** (lastCallSummary exists):
  - Show: "Voice call summary: [summary]" with a dismissable X button
  - Use a slightly different style (muted background, left border accent)
  - When dismissed, call onDismiss

- **No state**: return null

Style with Tailwind. Use `cn()` from `@/lib/utils`. Keep it compact — max height ~48px for pre-call, expandable for post-call summary.

Then find the main chat component (likely `components/chat/fred-chat.tsx` or similar — search for the component that renders the chat interface with the voice button) and integrate:

1. First, find the correct file. Search for where `VoiceChatOverlay` is used — that's the parent chat component.

2. In that parent component:
   - Add state: `const [lastDiscussedTopic, setLastDiscussedTopic] = useState<string | null>(null)`
   - Add state: `const [lastCallSummary, setLastCallSummary] = useState<string | null>(null)`
   - When initiating a voice call (wherever `/api/fred/call` is called), read `lastDiscussedTopic` from the response and set it
   - After the call ends (when LiveKit disconnects), collect the transcript from data channel messages and POST to `/api/fred/call/transcript`
   - Render `<VoiceCallContextBanner>` above the chat messages area

3. For the transcript collection on call end:
   - The LiveKit `@livekit/components-react` hooks provide access to data channel messages
   - Look for existing LiveKit room/connection logic in the codebase
   - Add a `useEffect` or callback that fires when the room disconnects
   - Collect all received transcript data channel messages and POST to `/api/fred/call/transcript`
   - Set `lastCallSummary` from the transcript for the post-call banner

IMPORTANT: If `fred-chat.tsx` doesn't exist or the voice call integration point is elsewhere, search the codebase. The VoiceChatOverlay is imported somewhere — find that file and integrate there. The file might be in `app/chat/page.tsx` or `app/dashboard/` somewhere.

Use `"use client"` directive. Import from `@/components/chat/voice-call-context-banner`.
  </action>
  <verify>
    Run `npm run build` to verify all components compile.
    Verify the banner component exists: `ls components/chat/voice-call-context-banner.tsx`
  </verify>
  <done>
    - VoiceCallContextBanner component renders pre-call topic and post-call summary
    - Banner is integrated into the chat interface
    - Transcript is POSTed to /api/fred/call/transcript when call ends
    - Post-call summary appears in chat as a dismissible banner
    - Build passes with no errors
  </done>
</task>

</tasks>

<verification>
1. TypeScript compilation: `npm run build` passes
2. Code review: voice transcripts are stored as episodic memory entries with channel='voice'
3. Code review: POST /api/fred/call/transcript endpoint exists and validates auth + ownership
4. Code review: VoiceCallContextBanner shows last discussed topic before calls
5. Code review: After call ends, transcript is POSTed and summary banner appears
</verification>

<success_criteria>
- Voice call transcripts are injected into the chat history via episodic memory
- Pre-call banner shows "FRED remembers: Last discussed — [topic]"
- Post-call banner shows a summary of what was discussed on the call
- Transcript POST endpoint validates that the caller owns the room
- All transitions between text and voice preserve context in both directions
</success_criteria>

<output>
After completion, create `.planning/phases/82-chat-voice-continuity/82-02-SUMMARY.md`
</output>
