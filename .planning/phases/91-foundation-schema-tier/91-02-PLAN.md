---
phase: 91-foundation-schema-tier
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/__tests__/tier-resolution.test.ts
  - lib/api/tier-middleware.ts
autonomous: true

must_haves:
  truths:
    - "getTierFromString('builder') returns UserTier.BUILDER (value 1)"
    - "canAccessFeature(UserTier.BUILDER, UserTier.BUILDER) returns true"
    - "canAccessFeature(UserTier.FREE, UserTier.BUILDER) returns false"
    - "canAccessFeature(UserTier.PRO, UserTier.BUILDER) returns true"
    - "getProfileTier returns BUILDER when profiles.tier = 1"
    - "getPlanByPriceId with BUILDER price ID returns the BUILDER plan"
    - "getUserTierFromSubscription returns BUILDER for a BUILDER price subscription"
    - "getUserTier returns BUILDER for a user with active BUILDER subscription (no profile override)"
  artifacts:
    - path: "lib/__tests__/tier-resolution.test.ts"
      provides: "Unit tests for BUILDER tier resolution across all code paths"
      min_lines: 80
    - path: "lib/api/tier-middleware.ts"
      provides: "Fixed getProfileTier that recognizes BUILDER tier"
      contains: "UserTier.BUILDER"
  key_links:
    - from: "lib/api/tier-middleware.ts"
      to: "lib/constants.ts"
      via: "getTierFromString, canAccessFeature, UserTier"
      pattern: "import.*UserTier.*from.*constants"
    - from: "lib/api/tier-middleware.ts"
      to: "lib/stripe/config.ts"
      via: "getPlanByPriceId"
      pattern: "import.*getPlanByPriceId.*from.*stripe/config"
---

<objective>
Unit test the BUILDER tier resolution across every code path and fix the getProfileTier gap that skips BUILDER.

Purpose: Pitfall C1 warns that if any tier resolution path silently falls through to FREE for BUILDER subscribers, users pay $39 and get nothing. The BUILDER tier constants exist but have never been tested. Additionally, `getProfileTier()` in `lib/api/tier-middleware.ts` lines 96-98 only returns STUDIO and PRO — it never returns BUILDER, meaning admin-assigned BUILDER tiers would silently become FREE.

Output: Comprehensive tier unit test file + one bug fix in tier-middleware.ts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/research/PITFALLS.md

@lib/constants.ts (UserTier enum, getTierFromString, canAccessFeature, getUpgradeTier — lines 37-189)
@lib/stripe/config.ts (PLANS object, getPlanByPriceId, getPlanById)
@lib/api/tier-middleware.ts (getUserTier, getProfileTier — THE BUG IS HERE lines 85-102)
@app/api/stripe/webhook/route.ts (getUserTierFromSubscription — lines 38-46, resolveUserIdFromSubscription — lines 237-252)
@vitest.setup.ts (test setup config)
</context>

<feature>
  <name>BUILDER Tier Resolution Tests + getProfileTier Fix</name>
  <files>lib/__tests__/tier-resolution.test.ts, lib/api/tier-middleware.ts</files>
  <behavior>
  Test the following behaviors:

  **getTierFromString (lib/constants.ts):**
  - 'builder' -> UserTier.BUILDER (1)
  - 'BUILDER' -> UserTier.BUILDER (case insensitive)
  - 'pro' -> UserTier.PRO
  - 'fundraising' -> UserTier.PRO (legacy alias)
  - 'studio' -> UserTier.STUDIO
  - 'venture_studio' -> UserTier.STUDIO (legacy alias)
  - 'unknown' -> UserTier.FREE (fallback)
  - '' -> UserTier.FREE (empty string)

  **canAccessFeature (lib/constants.ts):**
  - (BUILDER, BUILDER) -> true
  - (FREE, BUILDER) -> false
  - (PRO, BUILDER) -> true (higher tier can access lower)
  - (STUDIO, BUILDER) -> true
  - (BUILDER, PRO) -> false (lower tier cannot access higher)

  **getUpgradeTier (lib/constants.ts):**
  - FREE -> BUILDER
  - BUILDER -> PRO
  - PRO -> STUDIO
  - STUDIO -> null

  **getPlanByPriceId (lib/stripe/config.ts):**
  - With a mock BUILDER price ID -> returns plan with id 'builder'
  - With unknown price ID -> returns null

  **getPlanById (lib/stripe/config.ts):**
  - 'builder' -> returns BUILDER plan with price 39
  - 'fundraising' -> returns FUNDRAISING plan
  - 'nonexistent' -> returns null

  **BUG FIX — getProfileTier (lib/api/tier-middleware.ts):**
  Current code (lines 96-98):
  ```
  if (tier >= UserTier.STUDIO) return UserTier.STUDIO;
  if (tier >= UserTier.PRO) return UserTier.PRO;
  return null;  // <-- BUILDER (tier=1) falls through to null -> FREE
  ```
  Must become:
  ```
  if (tier >= UserTier.STUDIO) return UserTier.STUDIO;
  if (tier >= UserTier.PRO) return UserTier.PRO;
  if (tier >= UserTier.BUILDER) return UserTier.BUILDER;
  return null;
  ```

  **SUBSCRIPTION-BASED TIER RESOLUTION — getUserTier end-to-end (lib/api/tier-middleware.ts):**
  Test the subscription-based path through getUserTier (not the profile override path):
  - Mock a user with NO profile tier override (getProfileTier returns null)
  - Mock an active subscription with BUILDER price ID in the subscriptions table
  - Mock `getActiveSubscription` to return { stripePriceId: BUILDER_PRICE_ID, status: 'active' }
  - Mock `getPlanByPriceId(BUILDER_PRICE_ID)` to return the BUILDER plan
  - Call `getUserTier(userId)` — must return UserTier.BUILDER
  - This verifies the subscription-based resolution path returns BUILDER end-to-end through tier-middleware, not just the profile-based path
  </behavior>
  <implementation>
  RED: Write tests in `lib/__tests__/tier-resolution.test.ts` that cover all behaviors above. The getProfileTier test will FAIL because it currently does not return BUILDER. The getUserTier subscription-based test may also fail if the subscription path has the same gap.

  GREEN: Fix `getProfileTier` in `lib/api/tier-middleware.ts` by adding `if (tier >= UserTier.BUILDER) return UserTier.BUILDER;` before the `return null` on line 98. Then all tests pass.

  REFACTOR: Not needed — the fix is a one-liner.

  Note on testing getProfileTier: Since it calls Supabase, mock `createServiceClient` to return a fake Supabase client that returns `{ data: { tier: 1 }, error: null }`. Use `vi.mock('@/lib/supabase/server')`.

  Note on testing getUserTier subscription path: Mock `getActiveSubscription` from `@/lib/db/subscriptions` and `getProfileTier` (or its underlying Supabase call returning null). This ensures the test exercises the subscription lookup branch, not the profile override branch.
  </implementation>
</feature>

<verification>
- `npm run test -- lib/__tests__/tier-resolution.test.ts` passes all tests
- `npm run test` passes (no regressions in existing 1048 tests)
- `grep 'UserTier.BUILDER' lib/api/tier-middleware.ts` shows the fix in getProfileTier
</verification>

<success_criteria>
1. All tier resolution tests pass covering getTierFromString, canAccessFeature, getUpgradeTier, getPlanByPriceId, getPlanById
2. getProfileTier bug fixed — BUILDER tier (value 1) no longer falls through to null/FREE
3. getUserTier returns BUILDER via subscription-based path (no profile override) — tested end-to-end
4. Zero test regressions (existing 1048 tests still pass)
</success_criteria>

<output>
After completion, create `.planning/phases/91-foundation-schema-tier/91-02-SUMMARY.md`
</output>
