---
phase: 91-integration-and-agent-knowledge
type: execute
domain: nextjs-integrations
---

<objective>
Create the secure event, agent-task, and knowledge contracts approved in the
visual architecture.

Purpose: Let Sahara agents exchange structured tasks and attributable findings
without sharing credentials or bypassing tenant, consent, and approval rules.
Output: Versioned event envelope, policy gate, task/run evidence types, scoped
knowledge promotion contract, and tests.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/diagrams/visual-plan-20260724T204035-0700.md
@lib/agents/base-agent.ts
@lib/db/agent-tasks.ts
@lib/db/fred-memory.ts
@lib/fred/active-memory.ts
@lib/fred/tools/memory-search.ts
@lib/ai/memory-extraction-prompt.ts
@trigger
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the integration event and policy contracts</name>
  <files>lib/integrations/event-envelope.ts, lib/integrations/policy.ts, tests</files>
  <action>Create versioned schemas carrying event ID, correlation ID, tenant, provider, event type, subject reference, consent state, provenance, classification, and occurrence time. Add policy outcomes for allow, deny, quarantine, and human approval. Secrets and raw PII must be structurally excluded.</action>
  <verify>Schema tests reject missing tenant/correlation/provenance, secret-like fields, cross-tenant access, and unsafe actions without approval.</verify>
  <done>A versioned, validated, tenant-safe event and policy contract exists.</done>
</task>

<task type="auto">
  <name>Task 2: Define typed agent task, handoff, verifier, and evidence records</name>
  <files>lib/agents/task-contract.ts, lib/db/agent-tasks.ts, app/api/agents/tasks/route.ts, tests</files>
  <action>Extend the existing agent task surface with stable task/run/correlation IDs, capability requirements, typed blockers, handoffs, verifier outcomes, and redacted evidence references. Preserve current API compatibility and avoid direct agent-to-agent secret transport.</action>
  <verify>Tests cover create, handoff, retry, blocker, verifier pass/fail, tenant isolation, and backward compatibility.</verify>
  <done>Agents can coordinate and prove work through typed records rather than unlogged messages.</done>
</task>

<task type="auto">
  <name>Task 3: Add scoped knowledge retrieval and promotion rules</name>
  <files>lib/knowledge/broker.ts, lib/knowledge/promotion.ts, lib/fred/tools/memory-search.ts, tests</files>
  <action>Wrap existing Fred memory and pgvector retrieval with tenant, task-purpose, provenance, freshness, classification, and confidence fields. Add promotion rules for duplicate detection, evidence minimums, and human approval for sensitive/high-risk knowledge. Do not ingest credentials, direct payment data, or unverified agent speculation.</action>
  <verify>Tests prove scoped retrieval, cross-tenant denial, provenance/freshness output, duplicate rejection, and high-risk approval routing.</verify>
  <done>Agents receive attributable scoped knowledge and can promote only evidence-backed findings.</done>
</task>

</tasks>

<verification>
- [ ] Unit and integration tests pass twice
- [ ] Existing agent and Fred-memory tests remain green
- [ ] TypeScript, lint, and build pass
- [ ] No schema permits credential material in events, tasks, or knowledge
</verification>

<success_criteria>
- Event, agent-task, and knowledge contracts match the approved visual
- Existing APIs remain backward-compatible
- Tenant isolation, provenance, and approval rules are test-proven
</success_criteria>

<output>
After completion, create `.planning/phases/91-integration-and-agent-knowledge/91-02-SUMMARY.md`.
</output>
