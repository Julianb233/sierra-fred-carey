---
phase: 91-integration-and-agent-knowledge
type: execute
domain: nextjs-integrations
---

<objective>
Complete Sahara's member-lifecycle event and Customer.io reporting contracts.

Purpose: Cover every lifecycle state Fred named, make events replay-safe, and
establish signed delivery/readback evidence before any campaign can go live.
Output: Append-only lifecycle events, production emitters, signed reporting
webhook, tests, and an operator runbook.
</objective>

<execution_context>
@~/.claude/skills/create-plans/workflows/execute-phase.md
@~/.claude/skills/create-plans/templates/summary.md
</execution_context>

<context>
@.planning/diagrams/visual-plan-20260724T204035-0700.md
@.planning/DEPLOY-VERIFY-2026-07-24-sahara-customerio.md
@docs/customerio/member-lifecycle-production-setup.md
@lib/customerio/events.ts
@lib/customerio/member-lifecycle.ts
@app/api/onboard/route.ts
@app/api/stripe/webhook/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete Fred's append-only lifecycle event contract</name>
  <files>lib/customerio/events.ts, lib/customerio/member-lifecycle.ts, docs/customerio/member-lifecycle-production-setup.md</files>
  <action>Add explicit events and typed properties for first value, recommended action not viewed, onboarding started, deck submitted, upgrade abandonment, paid onboarding, and motivational eligibility. Preserve every existing event name. Require a stable dedupe key, consent state, source, and correlation ID. Do not copy Audience Lab/GHL lead state into Customer.io member profiles.</action>
  <verify>Focused Customer.io unit tests cover every event name, property type, dedupe rule, and member/lead boundary.</verify>
  <done>Every Fred-requested lifecycle state has one documented event and deterministic dedupe rule.</done>
</task>

<task type="auto">
  <name>Task 2: Wire real application emitters without duplicate sends</name>
  <files>app/api/onboard/route.ts, app/api/stripe/webhook/route.ts, app/api/cron/re-engagement/route.ts, lib/email/milestones/triggers.ts, relevant deck/recommendation routes</files>
  <action>Emit the completed event contract from the canonical Supabase-backed application paths. Upsert the member before event delivery. Make failures non-blocking for the core product but observable. Keep existing Resend/Twilio sends authoritative while Customer.io journeys remain paused.</action>
  <verify>Route and integration tests prove each event emits once, retries remain idempotent, and missing credentials skip safely.</verify>
  <done>All events with an existing product action have a real emitter; unavailable product states are explicitly documented rather than simulated.</done>
</task>

<task type="auto">
  <name>Task 3: Add signed Customer.io reporting webhook and evidence storage</name>
  <files>app/api/webhooks/customerio/route.ts, lib/customerio/reporting-webhook.ts, tests, docs/customerio/member-lifecycle-production-setup.md</files>
  <action>Implement an HTTPS POST receiver that verifies X-CIO-Signature using the configured signing key, rejects replay/invalid payloads, redacts PII, returns promptly, and records correlation-safe delivery, bounce, complaint, unsubscribe, and failure evidence. Never log raw message bodies, credentials, or direct contact identifiers.</action>
  <verify>Tests cover valid signature, invalid signature, replay/deduplication, malformed JSON, and every supported delivery event; production build passes.</verify>
  <done>The reporting webhook is signed, replay-safe, observable, and documented with a sandbox canary procedure.</done>
</task>

</tasks>

<verification>
- [ ] Focused Customer.io tests pass twice
- [ ] TypeScript and lint pass
- [ ] Production build passes
- [ ] No secret values or direct member identifiers appear in logs or fixtures
- [ ] Existing Sentry, Linear, Resend, Twilio, and Stripe behavior remains intact
</verification>

<success_criteria>
- Fred's complete lifecycle list is represented in the event contract
- Product emitters are idempotent
- Customer.io reporting is signature-verified
- Live campaigns remain paused
</success_criteria>

<output>
After completion, create `.planning/phases/91-integration-and-agent-knowledge/91-01-SUMMARY.md`.
</output>
